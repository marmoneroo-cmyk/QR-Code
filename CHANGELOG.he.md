# יומן שינויים

כל השינויים המשמעותיים בפרויקט מתועדים כאן.
הפורמט עוקב אחר [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

---

## [2026-07-10] — הקשחה מבוססת אודיט מערכתי (4 אודיטים במקביל → אצווה ממוזגת, חי)

4 אודיטים read-only במקביל (בסיס-נתונים/סקלביליות · עיצוב API · לכידות מוח-ה-AI · אוטומציה/תצפיתיות), מוזגו לתוכנית אחת, ובוצעו ע״י 3 executors במקביל. אומת: `tsc` + **205 בדיקות** + `next build` (57 עמודים) ירוקים; נבדק חי (`/api/health` `db:ok`, קריאה ציבורית `Cache-Control: s-maxage=60`, מעטפת תקינה, ללא שגיאות קונסול).

### תוקן — תצפיתיות (כשלים שקטים היו בלתי-נראים)
- **10 קריאות אנליטיקה** (queries×5, crm/journeys/recommendations/signals/tables) עטפו את Supabase ב-`catch { return ריק }` **בלי log** — כשל DB/RLS/timeout החזיר נתונים ריקים מזויפים כ-HTTP 200, בלתי-נראה (גם ה-Integrity + Signal-Verify הסתירו את הכשלים שלהם). כעת כל אחד רושם `log.error` לפני ההחזרה הריקה (ללא שינוי ערך).
- `/api/track` רושם dropים של slug לא-מוכר; `/api/health` הוסיף `lastEventAgeSeconds`.
- **ניקוי NUL:** ל-`recommendations.ts` היו 111 בייטים של NUL (מפריד pairKey) → הקובץ נראה כ-diff בינארי, בלתי-נראה לסקירה. הוחלף ב-`|`.

### תוקן — הקשחת API (9 נתיבים, ללא תלויות חדשות)
- **ולידציית קלט:** `experience` PUT מאמת עכשיו את ה-config לפני שמירה (הוא מוגש מחדש לתפריט הציבורי); `promotions` PATCH מריץ את השדות דרך אותן בדיקות כמו POST; `sales` POST מאמת כל שורה + תקרה 1000; `changes` מאמת `date` לפני `new Date()`.
- **Caching:** `Cache-Control` על 4 הקריאות הציבוריות (promotions/experience/recommendations GET + poster) — שהיו round-trip ל-DB בכל צפייה בתפריט.
- **עקביות:** 3 נתיבים אוחדו למעטפת `{success,data|error}`; scrape-restaurant מחזיר **502** ליעד לא-נגיש; import-restaurant רושם כשלי תמונה.

### שונה — עקביות מוח-העסק (dedup)
- קובץ חדש `src/lib/format.ts` (`formatILS`); **7** עותקים של פורמט ה-₪ אוחדו. `confidenceFrom`/`confidenceFromViews` (זהים, **defaults שונים 10 מול 30**) → `confidenceBucket()` משותף אחד; כל קורא שומר את ה-default שלו.

### נוסף — מיגרציית `perf(db)` (צריך להחיל)
- `supabase/migrations/0012_member_user_index.sql` — index על `restaurant_members(user_id)`, נתיב האימות החם.

**תויק להמשך (צריך גישת-DB / החלטת-מוצר):** aggregation views + RLS `(select auth.uid())` + ייחודיות sales; ה-`diagnoseFunnel` היתום + 3 מנועי המלצה מקבילים; איחוד logger איזומורפי; scoping של promotions/experience לפני דייר #2.

---

## [2026-07-10] — בידוד דיירים באנליטיקה + יושרת מדידה + תצפיתיות (חי בפרודקשן)

### אבטחה — קריאות אנליטיקה מוגבלות-דייר (B5 · סקירת אבטחה H3, השער לפני דייר #2)
- פונקציות ה-lib של האנליטיקה כבר קיבלו פרמטר מסעדה וסיננו לפי `restaurant_id`, אבל **כל נתיב קריאה מאובטח התעלם מה-session ונפל לברירת המחדל `'diner'`** — משתמש מחובר של דייר שני היה רואה את הנתונים של diner. כעת כל נתיב קריאה מאובטח מעביר `session.restaurantSlug`: overview, menu-engineering, crm, executive, heatmap, opportunities, sessions, `events/raw`, `events/integrity`, `signals/verify`, `experiments/results`. אומת: **אפס קריאות `events` לא-מוגבלות** בכל ספריות האנליטיקה. השער לקליטת דייר #2 נפתח.

### אבטחה — אין דליפת שגיאות פנימיות + לוגים מובנים בשרת (סקירת אבטחה M2)
- קובץ חדש `src/lib/log.ts`: logger מובנה מינימלי (`level`/`scope`/`ts`). מחובר בשני התפרים שרואים את כל שטח ה-API: `guard.unauthorized()` רושם עכשיו כל שגיאת 500 בלתי-צפויה **במלואה בצד השרת** ומחזיר ללקוח הודעה **גנרית `internal error`** (הודעות SQL/טבלה פנימיות כבר לא דולפות בגוף התשובה); `/api/track` רושם כשלי קליטה עם slug וגודל אצווה ומחזיר `ingest failed` גנרי.

### תוקן — ביטחון (confidence) אמין, נגזר ממדגם אמיתי (F6)
- נמחקה טבלת ה-confidence הקבועה `{low:58, medium:74, high:91}`. `confidencePct` נגזר עכשיו מ**המדגם הנצפה האמיתי** (צפיות עמוד המשקה) דרך עקומת הרוויה `n/(n+60)` של מנוע המשפך, מעוצב לפי ההפרדה האיכותית ותקרה של 95 — 100 צפיות כבר לא יכולות לטעון 91% ודאות, ואפס נתונים נקרא ביושר 0%.

### תוקן — שער מדגם-קטן לאחוזים (F4)
- אחוז הזמנה שמחושב ממכנה זעיר ("100% הזמינו" מ-2 צפיות) מזייף ביטחון. קובץ חדש `src/lib/analytics/rate.ts` (`hasConfidentSample`, שער = 25 צפיות). הוחל אחיד על כל תצוגת אחוז-הזמנה (KPI ביצועי הבית, כרטיסי פריט בניתוח התפריט, KPI וטבלת האנליטיקה): האחוז המקורי מוצג כשהמדגם עובר את השער, אחרת `—` מעומעם. החישוב המצטבר לא השתנה. 7 בדיקות.

### אבטחה — סבב הקשחה (מבוסס אודיט)
- **הגנת SSRF.** ה-scraper הביא URL מהמשתמש עם מעקב-הפניות ברירת-מחדל וללא סינון IP — קורא מאומת יכול היה להגיע ל-`169.254.169.254` (מטא-דאטה של הענן → גניבת הרשאות) או לכל מארח פנימי. קבצים חדשים `src/lib/net/ip.ts` (מסווג טהור ובדוק) ו-`src/lib/net/ssrf.ts` (`safeFetch`: מאמת מארח, מסרב לכתובות חסומות, עוקב אחר הפניות **תוך אימות מחדש בכל קפיצה**, timeout 10ש'). 8 בדיקות.
- **Path traversal.** `generate-breakdown` שילב את `body.slug` ישירות לשם קובץ בשרת; `slugify()` מגביל עכשיו ל-`[a-z0-9-]`.
- **הגבלות ניצול קליטה.** `/api/track` הציבורי דוחה עכשיו גוף גדול מדי (64KB → 413) לפני פענוח ומסרב fetch חוצה-אתר מפורש (403), בלי לחסום `fetch`/`sendBeacon` מאותו מקור. (הגבלת קצב לפי IP דורשת KV — follow-up.)
- **סוד + headers.** נתיב השרת כבר לא נופל ל-`NEXT_PUBLIC_POLLINATIONS_TOKEN` (שנארז לדפדפן); `esc()` מקודד גם גרש בודד; נוספו security headers בסיסיים ב-`next.config.ts`.

### נגישות — סבב WCAG על מסכי הפרימיום
- **הפחתת תנועה (שורש).** `<MotionConfig reducedMotion="user">` עוטף עכשיו את כל האפליקציה — כל רכיב Framer Motion מכבד את הגדרת ה-OS, כולל מסכים שמשתמשים ב-variants המשותפים.
- **פוקוס ומקלדת.** ה-launcher וספריית המדיה מעבירים פוקוס לדיאלוג בפתיחה, לוכדים Tab, משחזרים פוקוס בסגירה, ונסגרים ב-Escape. `AdminTabs` מיישם את דפוס ARIA Tabs המלא (אומת חי). לחצן השפה מקבל `aria-pressed`; שדה שם-המסעדה מקבל `<label>` מקושר.

### תוקן — מצבי טעינה עמידים (שגיאה ≠ ריק)
- כל מסך ניהול בלבל כשל-רשת עם "אין נתונים עדיין" — ובשני מסכים היו unhandled rejections. primitive חדש `ErrorState` (התראה נפרדת + ניסיון חוזר) חובר ל-**10 מסכים** כך שכשל מציג שגיאה עם retry, לא מצב ריק מטעה.

**אימות:** `tsc` + **183 בדיקות** + `next build` (57 עמודים) ירוקים; חמישה deploys מאומתים. בדיקות חיות: תפריט ציבורי 200 ללא שגיאות קונסול, כניסה עובדת, ARIA של AdminTabs תקין בדפדפן, פוקוס+Escape של ה-launcher אומתו, security headers נוכחים.

---

## [2026-07-06] — תשתית מרובת-דיירים + עיצוב פרימיום (חי בפרודקשן)

### אבטחה — תשתית מרובת-דיירים (Epics A+B+C+D)
- **אימות נאכף.** `requireSession()` שומר על כל `/api/*` של הניהול; `/admin/*` חסום מאחורי כניסה (`AUTH_ENFORCED=true`, חי). נתיבי הסועד הציבוריים (`promotions`/`experience`/`analytics-recommendations` GET, `/api/track`) נשארים פתוחים בכוונה.
- **בידוד דיירים.** ה-tenant נגזר עכשיו מה-session — נסגרה דליפת הכתיבה החוצה-דייר של `?restaurant=`; כתיבות ניהול עוברות דרך client מוגבל-משתמש כך ש-**RLS של Supabase הוא הגבול החי**. מיגרציות `0007`/`0008`/`0011` הוחלו.
- **צינור נתונים אמין.** תור אירועים at-least-once (localStorage + retry + `sendBeacon`) + idempotency בשרת (`event_id` upsert `ON CONFLICT DO NOTHING`) = אפקט exactly-once; חותמות מקור (`eventVersion`/`eventSource`/`uiVersion` + `restaurantType`/`menuCategory`) על כל אירוע; מעטפת מקור להמלצה.

### נוסף — עיצוב פרימיום "Obsidian & Champagne" (שכבת תצוגה, אפס שינוי לוגיקה)
- מערכת עיצוב + ספריית פרימיטיבים (`src/components/ui/premium.tsx`): `GlassCard`, `CtaPill`, `StatBlock`, `EmptyState`, `AmbientBackdrop`, `GlowDivider` — שחורים אובסידיאן, מבטא שמפניה, משטחי זכוכית, תנועה קולנועית.
- ניווט קפסולת-זכוכית + launcher קולנועי (`AdminShell`/`AdminLauncher`); מסך בית flagship; תפריט אורחים תמונה-קדימה (אריחים מובילים כפולים + פוטרי זכוכית ב-`MenuRow`); מסכי AI קולנועיים; סוויפ מלא של ~24 מסכי ניהול.
- **ספריית מדיה** (`MediaLibrary`) — בורר תמונת-hero בסגנון Figma/Lightroom (תפריט · טיוטות · העלאה · אחרונים, חיפוש חי, גרירה, מגבלת 4MB), משולב ב-`CocktailForm` דרך ה-`setHeroUrl` הקיים.
- תיקון RTL גלובלי (`DirectionSync`) — `<html dir/lang>` עוקב אחר שפת הממשק בכל עמוד.

### שונה — איחוד IA של הניהול (23 פריטי launcher → 6 יעדים + מתקדם)
- אורגן מחדש ל-6 יעדים ראשיים + מתג **מתקדם** (נשמר): **פעולה** (בית · היום · תוצאות), **תפריט וקידום**, **תובנות**, **כלים**, וקבוצת **מתקדם** מוסתרת.
- יעדים חדשים עם טאבים (`AdminTabs`, טעינה עצלה לטאב הפעיל): `/admin/today` · `/admin/results` · `/admin/promote` · `/admin/tools`. התוכן הפנימי חולץ כ-`*Panel` — **כל route מקורי עדיין עובד** (סימניות לא נשברו).

### תוקן — מדידת לולאה-סגורה כנה (F1)
- `measureImpact` כבר לא ממציא ניצחונות: `too_early` עד שחלון post שלם ושווה-אורך חולף; `insufficient_data` על בסיס אפסי/זעיר; `no_effect` מתחת לרצועת הרעש / דלתא מוחלטת מינימלית. מנוע 5-סטטוסים מחווט מקצה-לקצה; 10 בדיקות.

### נוסף — סקריפטי פיתוח / אימות
- `scripts/audit/` — הרנסי אימות חיים (גבול auth 401, idempotency, membership, segments). קוראים סודות מ-`.env.local` (ב-gitignore); אף אחד לא מקובע בקוד.

**אימות:** `tsc` + **167 בדיקות** + `next build` (57 עמודים) ירוק; פרודקשן נבדק (ציבורי 200 · APIs חסומים 401 · `/admin/*` → כניסה). פרוק: F2–F6 של Epic F, המשך B5 (בידוד קריאות אנליטיקה לפני דייר #2).

---

## [Unreleased] — 2026-06-08

### נוסף — השלמת בקלוג (ליטוש עורך · סידור CMS · תנועה ציבורית · בדיקות)

- **עורך הקוקטייל** עוצב מחדש לשפה היוקרתית (CocktailForm + new/edit); נתיב העריכה נופל-חזרה לקוקטייל מ-MENU (prefill) במקום מבוי-סתום "לא נמצא".
- **סידור ב-Composer:** גרירה לסידור טיוטות (localStorage); **גרירה לסידור התפריט שפורסם** דרך hook חדש `useMenuOrder` (נשמר, פר-מכשיר) — משתקף בתפריט הסועד עם נפילה-חזרה בטוחה (בלי סדר מותאם ⇐ ללא שינוי), עם בקרת "סדר מותאם · אפס".
- **Import** — stepper התקדמות; **QR/Print** — מיתוג אקסנט זהב (נאמנות הדפסה נשמרה).
- **מסכים ציבוריים:** אנימציית כניסה אדיטיבית (בלוקי סינון בתפריט, רמזי kiosk) — בלי אנימציה כפולה (MenuCard כבר מנפיש את עצמו לפי index).
- **בדיקות:** בדיקות יחידה ב-vitest ל-`eventTypeLabel`/`deviceLabel`, `deltaPct`, ו-`applyOrder` — 76 בדיקות ב-10 קבצים, כולן ירוקות.

### נוסף — ספרינטים של שדרוג ויזואלי (תנועה · גרפים אמיתיים · פיצ'רים לפעולה · ויזואליזציות ייחודיות)

ארבעה ספרינטים שהעלו את כל משטח הניהול ל"רמה הכי גבוהה", כל אחד אומת (`tsc` + `next build` נקי)
ונפרס:
- **ספרינט A — חבילת Level-Up (גלובלי):** אנימציית כניסה (fade) לכל תוכן הניהול (`AdminShell`),
  ספירת-עלייה (count-up) למספרי KPI + hover אחיד (`KpiCard`), מצבי טעינה עם shimmer
  (`Skeleton`/`SkeletonGrid`) במקום הטקסט "טוען…", `LiveDot` במסכים עם רענון, ואנימציית כניסה
  מדורגת לכרטיסים. פרימיטיבים משותפים חדשים ב-`dataviz.tsx` + `components/ui/motion.tsx`;
  keyframe `shimmer` + כיבוד `prefers-reduced-motion` ב-`globals.css`.
- **ספרינט B — גרפים אמיתיים:** `AreaChart` חלק ב-SVG (מילוי גרדיאנט, נקודת-קצה זוהרת, רספונסיבי)
  מחליף את ה-sparklines מבוססי הקוביות בכל מקום (כולל KpiCard); ב-Analytics גרף שטח כפול
  (צפיות/הזמנות) + בורר תקופה בצד-לקוח (7/14/הכל); מגמת ביקוש ב-Executive וגרף תנועה ב-Home.
- **ספרינט C — מהמילה לפעולה:** "תקציר הבוקר" של ה-AI ב-Executive (דטרמיניסטי, נתונים אמיתיים)
  + שיתוף תמונת מצב; רשימת-משימות ב-Opportunities (בוצע/דחה/הזכר-מחר, נשמר ב-localStorage,
  מקטע "טופל"); כפתורי "החל" ב-Optimize/Recommendations → עורך עם prefill; prefill ב-Promotions
  דרך `?cocktail=` + תצוגה מקדימה חיה; תצוגת אורח חיה במסגרת טלפון ב-Experience.
- **ספרינט D — ויזואליזציות ייחודיות:** מפת-רצפה ב-Tables (גודל = הכנסה, חום = המרה, כתר לשולחן
  המוביל); Heatmap כשכבת-חום על מוקאפ של עמוד הקוקטייל; משפך-מסע מצרפי ב-Journeys עם אחוזי
  נשירה; מטריצת Menu-Engineering אינטראקטיבית (popover במעבר עכבר + קליק לגלילה והדגשה).

הכל נתונים אמיתיים בלבד (תחזיות מסומנות כהערכה), דו-לשוני עב/EN, ללא גלילה אופקית.

### שונה — שיפוץ יוקרתי של כל משטח הניהול ("מערכת ההפעלה של המסעדה מבוססת ה-AI")

עוצב מחדש **כל מסך ניהול** ממרכז BI/Jira גנרי למוצר אחד יוקרתי, מונחה-תמונה, של "יועץ מסעדה
מבוסס AI", בהתאם למשוב הבעלים ("מקצועי כן, WOW לא"). ספריית פרימיטיבים משותפת חדשה
`src/components/ui/dataviz.tsx` — `GlassImage` (רינדור קוקטייל שלם, לעולם לא חתוך, בזכוכית
שקופה עם זוהר דגש; התיקון לבאג הזכוכית החתוכה היה מיכל `relative` + `absolute inset-0 object-contain`
בגובה **מוגדר**), `KpiCard`, `Sparkline`, `deltaPct`, `Pill`, `ConfidenceBadge`, `SectionLabel` — אומצו
בכל המסכים כך שהחבילה מרגישה כמוצר יוקרה אחד.

עוצב מחדש מסך-אחר-מסך (כל אחד: נתונים אמיתיים בלבד — תחזיות מסומנות כהערכות, ללא מספרים מומצאים;
דימויי קוקטייל דרך `findCocktailBySlug`/`getAccent`; **ללא גלילה אופקית** בשום מקום; דו-לשוני אנגלית/עברית;
TS-strict, ללא `any`):
- **Executive** (תבנית), **Home** (משגר אייקונים)
- **Opportunities, Closed Loop, Analytics, Optimize, Signals, Sales** (גל 1)
- **Menu Engineering** (מטריצת 2×2 יוקרתית עם זכוכיות ממוקמות), **Tables, Journeys, Heatmap,
  Recommendations, Experiments** (כן הוגן "אוסף נתונים", ללא מנצחים מזויפים), **CRM/Audience** (גל 2)
- **Promotions, Experience** (הטפסים נשמרו בית-אחר-בית), מפקח **Events** + סרגלי כרטיסיות יוקרתיים
  ב-**Menu Analysis** + **Guests** (גל 3)
- **Composer, QR, Print kit, Import** (גל 4 — נאמנות הדפסה + כל לוגיקת ה-CRUD/העלאה/גרידה נשמרה)

הניווט נוקה והפך ל**משגר לוח מחוונים מבוסס אייקונים** (`AdminLauncher` + מודל `Sections` ב-`AdminShell`);
אייקון האדם בתפריט פותח כעת את `/admin/home` (היה העורך). אחוז ההמרה מוצג כמספר שלם.

**אימות:** `tsc --noEmit` + `next build` נקיים בכל ארבעת הגלים; כל **22 נתיבי הניהול נבדקו בזמן ריצה
בדפדפן** (אפס שגיאות רינדור, נתונים אמיתיים, מצבים ריקים חיננים); נפרס לייצור לאחר כל גל.

### תוקן — שמירת CMS הניהול (QA טרום-הדגמה)

ביקורת טרום-הדגמה גילתה שה-composer לא היה מסוגל לשמור בפועל: כאשר Supabase מוגדרת, כתיבות/קריאות
של טיוטות עברו דרך **מפתח ה-anon** ונחסמו על ידי RLS (אין התחברות בצד הסועד) — פריטים הופיעו אופטימית
ואז נעלמו בטעינה מחדש. מאגר הטיוטות הוחלף ל-**localStorage כברירת מחדל** (התפריט החי הוא סטטי;
טיוטות Supabase דורשות אימות חבר). אומת מקצה לקצה בדפדפן אמיתי: יצירת קוקטייל + העלאת תמונה →
נשמר (התמונה כ-data URL) → מופיע בתפריט → שורד טעינה מחדש. טיוטות Supabase נשארות זמינות מאחורי
`NEXT_PUBLIC_USE_SUPABASE_DRAFTS=true` ברגע שאימות קיים. הסתייגות: טיוטות localStorage הן לכל-מכשיר.

כמו כן אומת (סימולציית אנליטיקה רב-משתמשית, נוקה לאחר מכן): ספירת סשנים ייחודיים (משקה שנפתח פעמיים
בסשן אחד נספר פעם אחת), הכנסות/רווח מ-snapshots של מחירים, שיוך QR לשולחן + פיצול ויראלי. כל 33 הדפים
ו-19 ה-APIs מחזירים מצב תקין.



### נוסף — לוח מחוונים ראשי + איחוד ניווט (חוויית הבעלים)

בעקבות ביקורת חוויית הבעלים — פחות יעדים, החלטות טובות יותר.
- **לוח מחוונים ראשי** (`/admin/home`, כעת פריט הניווט הראשון/הראשי) — "מה כדאי לי לעשות
  היום?" במסך אחד. 7 ווידג'טים, **הרכבה טהורה של APIs קיימים** (ללא חישוב חדש):
  Top Opportunities · **Impact Tracker** (הישגי Closed Loop) · Business Health · Traffic Trends
  (sparkline) · Promotion Status · Menu Performance · Recent Changes. כל אחד מקשר למסך העומק שלו.
- **איחוד ניווט:** הסרגל השטוח בן ~24 הפריטים → **8 פריטים ראשיים** + **"More ▾"** מקובץ
  (Understand / Setup / Advanced). **Advanced מוסתר כברירת מחדל** (הבעלים לעולם לא רואה אותו אלא
  אם פותח את More). **Audience/CRM הוסר** מהניווט לחלוטין (הדף נשמר, ללא קישור) עד שיהיה
  `visitor_id` יציב + סגמנטציה אמיתית.
- המיצוב מוסגר מחדש כ-**Menu Optimization** (בעלים קונים הכנסות + החלטות, לא "אינטליגנציה").

**מיזוגי סביבות עבודה (בוצעו):** **Menu Analysis** (`/admin/menu-analysis`) = Optimize + Menu
Engineering + Heatmap ככרטיסיות ("למה המשקה הזה מתנהג כך?"); **Guests**
(`/admin/guests`) = Tables + Journeys ככרטיסיות ("מי היו האורחים?"). גוף כל מסך
חולץ לייצוא `*Panel` בעל שם; הנתיבים העצמאיים נשארים כעוטפי `AdminShell` דקים (קישורים עמוקים
ממשיכים לעבוד). ניווט: Optimize קופל אל מחוץ לראשי; קבוצת Understand היא כעת
**Menu Analysis · Guests · Pairings**. תוכנית ביקורת חוויית הבעלים הושלמה.

### נוסף — Closed Loop Optimization v1 (המלצה → פעולה → תוצאה נמדדת)

הופך את המוצר מאנליטיקה ל**אופטימיזציה**: הוא מוכיח כעת אם שינוי עבד.
- **מנוע מדידה** (`src/lib/closedloop`, 7 בדיקות) — משווה קצבי מטריקה יומיים
  לפני מול אחרי שינוי (הוגן לחלונות חלקיים), מבוקר-יושרה: `too_early` /
  `insufficient_data` כאשר המדגם/החלון חלש — **לעולם לא מספר מומצא**. אפס-בסיס
  מטופל בכנות. כל תוצאה נושאת **רמת ביטחון + חלון תצפית** (כך שבעלים לא
  מגיבים יתר על המידה למדגמים קטנים).
- **טבלת audit גנרית `changes`** (מיגרציה `0010`) — `change_type · entity_type · entity_id ·
  before · after · source · created_at`. ציר זמן רב-שימושי של כל מה שהשתנה, לא טבלה
  ייעודית ל-Closed Loop בלבד.
- **לכידת שינוי אוטומטית** היא ברירת המחדל: יצירה/עריכה/הפעלה/מחיקה של מבצע ועדכוני
  experience מתעדים אוטומטית שינוי עם חותמת זמן. `logChange` הוא מאמץ-מיטבי — **כתיבת audit
  שנכשלה לעולם לא שוברת את הפעולה** (אומת: מבצעים עדיין נוצרים גם בהיעדר `0010`).
- **תיעוד ידני רק לפעולות חיצוניות** (תפריט מודפס, קמפיין אינסטגרם, צילומים).
- משטחים: **`/admin/closed-loop`** (תוצאות נמדדות + ציר זמן שינויים + טופס תיעוד ידני).
  API: `/api/closed-loop`, `/api/changes`. 56 בדיקות בסך הכול.
- v1 פשוט במכוון — Before → Action → After + Confidence. ללא שיוך רב-גורמי.

**ממתין:** הרץ את `supabase/migrations/0010_changes.sql` כדי להפעיל. הבא בתור לפי ביקורת חוויית
הבעלים: לוח מחוונים ראשי (עם ווידג'ט ה-Impact Tracker) → איחוד ניווט → סביבות עבודה Menu Analysis / Guests.

### נוסף — Opportunity Board (פעולות במקום לוחות מחוונים)

**Opportunity Board** (`/admin/opportunities`) — משטח הבוקר של "מה כדאי לי לעשות היום?".
מנוע טהור ובדוק (`src/lib/opportunities`, 8 בדיקות) הופך אותות מורכבים להזדמנויות מוקלדות
ומתועדפות, כל אחת עם **type · confidence · evidence · suggested action** ושער
היושרה (ספירות ראיות אמיתיות, פעולה מבוססת-כיוון בלבד, **ללא הכנסות מומצאות**).
- סוגי הזדמנות: `fix_offer` (מעורבות גבוהה + כוונה נמוכה), `promote_position` (נראות נמוכה
  + מעורבות גבוהה — **משתמש במיקום בתפריט**, מחזק את Blind Spots),
  `promote_marketing` (שיתוף גבוה + מכירות נמוכות — **Share Intelligence**), `promotion_candidate`
  & `reengage_returning` (**Returning-Visitor Intelligence** דרך `visitor_id`).
- **Menu Layout Intelligence** על אותו לוח: נשירת עומק-גלילה (הגיע לפריט 3+ / חצי /
  סוף), הישג קטגוריה, ופריטים שמגיעים אליהם לעיתים רחוקות.
- שאילתת שרת חדשה `getMenuSignals()` מרכיבה אותות לכל פריט מאירועים גולמיים (סשנים/מבקרים
  ייחודיים) + מיקום בתפריט + `visitor_id` (חוזר / חוזר-ללא-מחויבות) + טבלת ה-`sales`. API: `/api/analytics/opportunities`.
- **תיקון יושרת נתונים:** הוגבלו כל קצבי המעורבות ל-≤100% — חשיפות נספרות בחסר
  (QR/קישורים ישירים, "צפו גם", מועדף/שיתוף ברמת הכרטיס), מה שיצר
  "שיעור פתיחה של 143%" חסר היגיון. 49 בדיקות בסך הכול.

### נוסף

**סרטוני קוקטייל ל-Pinky + Garden Spritz** (`diner-pinky`, `garden-spritz`)
- קליפ יוקרתי אחד לכל קוקטייל (`/cocktail/video/diner-pinky.mp4` 3.6 MB, `/cocktail/video/garden-spritz.mp4` 3.65 MB), כל אחד מחובר **גם** כסרטון ה-hover של כרטיס התפריט **וגם** כמודל פיצ'ר ה-▶ Video בתוך המשקה, בשיקוף הגדרת ה-Aperol Spritz.
- אף נתיב קוד לא השתנה — `MenuCard`, `CocktailScene` ו-`MobileCocktailScene` כבר מזהים סרטון לפי slug דרך `getHoverVideo`/`getFeatureVideo`; רק רשומות נתונים ב-`COCKTAIL_VIDEOS` נוספו.
- אומת בייצור: סרטונים `200 video/mp4`, תפריט הבית מפנה לשני הקליפים, דפים `200`.

### שונה

**OrderBar → "My Picks" (משטח כוונה כן, לא הזמנה)**
- בהתאם לכיוון Menu Intelligence (`docs/MENU_INTELLIGENCE_PLAN.md`): הפלטפורמה היא אינטליגנציה, לא מערכת POS/הזמנות. הסרגל בתוך האפליקציה כבר לא מזייף מימוש.
- הוסרו פעולת **"Call waiter"** ואישור ההזמנה המזויף **"Sent ✓"**. הניסוח הוא כעת שפת כוונה/בחירות: *Add to my picks → Show waiter · N → In your picks ✓*.
- עדיין לוכד **כוונת** רכישה (`add_to_order_clicked` / `order_started` / `order_completed`) עם snapshot של מחיר/עלות — מסומן כעת כ-**intent value** ("הכנסה פוטנציאלית"), לעולם לא "מכירות בפועל" (מכירות אמיתיות מגיעות מאוחר יותר דרך קליטת POS לקריאה בלבד).

### הוסר

**כל קוד ה-360° המת** (360 הוחלף בסרטונים מוקדם יותר)
- נמחקו הרכיבים `Cocktail360.tsx`, `Cocktail3D.tsx`, `Spin360.tsx` והנתיב `/spin/[slug]`.
- הוסרו `COCKTAILS_WITH_3D`, `has3DModel()`, `model3dUrl()` מ-`src/data/cocktail.ts`.
- אומת: אפס הפניות חיות לפני המחיקה; `/spin/…` כעת `404`, **AR (`/ar/…`) נשמר ומחזיר `200`**, `tsc` + `build` ירוקים, דפי ייצור `200`.

### נוסף — יסודות Menu Intelligence

בניית הפלטפורמה מתוך `docs/MENU_INTELLIGENCE_PLAN.md` (אינטליגנציה + אופטימיזציה, לא הזמנות). נוסף **vitest** — הבדיקות הראשונות במאגר (**35 עוברות**).

**ליבת תזמון** (`src/lib/scheduling`) — פרימיטיב חלון-הזמן המשותף האחד (חוזר / טווח-תאריכים / עונתי), נכון-DST דרך `Intl`, מוערך ב-**אזור הזמן של המסעדה** עם `now` ניתן-להזרקה. בשימוש מחדש על ידי כל מה שניתן לתזמן. 10 בדיקות.

**שכבת Menu Experience** (`src/lib/experience`) — מתגי מודול-תוכן לכל פריט + תגים (Signature, Guest Favorite, Trending, Happy Hour, Discount, Seasonal, Limited Time, New, Custom), כל אחד הפעלה/השבתה/**ניתן-לתזמון**; תגים ידניים מול **מונחי-נתונים (אוטומטיים)**. 8 בדיקות.

**מנוע מבצעים** (`src/lib/promotions`) — הנחות אחוז/קבועות, היקף פריט/קטגוריה/הכול, מתוזמנות; בוחר את המחיר הפעיל הטוב ביותר; **כל מבצע פעיל מניע את התג שלו** (מקור אמת יחיד). 8 בדיקות.

**שכבת Menu Optimization** (`src/lib/optimization`) — הופכת נתוני menu-engineering ל**פעולות** של הבעלים (fix offer / promote position / raise price / review / keep), כל אחת עם רציונל + רמת ביטחון. **שער יושרה:** הערכת השפעה מספרית מופיעה רק כשהיא ניתנת-לגזירה וכשהנתונים תומכים בה — פריטים דלי-נתונים אומרים "collect more data", לעולם לא אחוז מומצא. 9 בדיקות. מוצג ב-**`/admin/optimize`** ("פעולה, לא מטריקות").

**בצד הסועד:** תגים + תמחור מבצע מיושם-אוטומטית (מקור עם קו מחיקה → מחיר מוזל) על כרטיסי התפריט, מחושב באזור הזמן של המסעדה. תצורת התחלה ב-`src/data/experience.ts` (Aperol = Signature, Pinky = New ליוני, Garden Spritz = Summer −15%, Friday Happy Hour −20%).

**תצורת בעלים — כעת מונחית-DB (ללא צורך במפתח):**
- **מנוע מבצעים** (חי ואומת מקצה לקצה מול ה-DB): עורך CRUD `/admin/promotions` + `/api/promotions` (קריאה ציבורית, כתיבת אדמין דרך service-role) + טבלת `promotions` (מיגרציה `0007`, הוחלה). הנחות מיושמות אוטומטית בתפריט ומדליקות את התג שלהן כשהן חיות; תפריט הסועד טוען אותן דרך `useMenuConfig` (נופל חזרה לברירות המחדל בקוד). אומת: יצירה → רשימה → מחיקה לא משאירה שאריות.
- **בונה Menu Experience**: `/admin/experience` (החלפת תגים + מודולי תוכן לכל קוקטייל) + `/api/experience` + טבלת `menu_experience` (מיגרציה **`0008` — ממתינה**, מכיוון שהתפריט החי סטטי ולכן `cocktails` ריקה). עד שתרוץ `0008`, ה-API מחזיר 500 בחן והתפריט משתמש בברירות המחדל בקוד.
- איחוד מבדל `Schedule` בשימוש מחדש בכל מקום; מזהה המסעדה נפתר + נשמר במטמון בצד השרת. טיפוסי `Database` הורחבו עבור שתי הטבלאות.

**גילוי בצד הסועד + מכירות (מעבר שלישי — `0008` הוחלה ואומתה):**
- **בונה Experience** אומת מקצה לקצה (הלוך ושוב PUT/GET); **מבצעים** אומתו מקצה לקצה (יצירה→רשימה→מחיקה נקי).
- **"Guests also viewed"** (`AlsoViewed`) — מכירה צולבת מונחית-נתונים מהתנהגות צפייה-משותפת אמיתית (`/api/analytics/recommendations`) בדפי הקוקטייל בדסקטופ ובמובייל; לא מרנדר דבר ללא נתונים אמיתיים.
- **מסנן מצב רוח** ("מה מתאים לי עכשיו?") — `sweet / sour / refreshing / strong / herbal` נגזר מפרופיל הטעם (`src/lib/mood.ts`, 6 בדיקות), מחובר למסנן רשת התפריט.
- **קליטת מכירות לקריאה בלבד** — `/admin/sales` (ייבוא הדבקת CSV) + `/api/sales` + טבלת `sales` (מיגרציה **`0009` — ממתינה**, RLS לחברים בלבד). מסך **Optimize** מציג יחידות/הכנסות בפועל לכל פריט כשמכירות קיימות (מתדרדר בחן ללא `0009`). זהו מכנה ההמרה.
- חבילת בדיקות: **41 עוברות**.

**ליטוש גילוי + מפת חום (מעבר רביעי — `0009` הוחלה ואומתה):**
- **קליטת מכירות** אומתה מקצה לקצה (POST→GET→ניקוי); Optimize מציג מכירות בפועל כשקיימות.
- **סיפורים לכל קוקטייל** (`CocktailStory`) בדף הקוקטייל (דסקטופ + מובייל).
- **רמז שעת היום** ("Perfect for sunset" / "Nightcap hour" …) בתפריט, באזור הזמן של המסעדה.
- **Top Picks** (Most Viewed + Hidden Gem) — מונחה-נתונים, מוסתר ללא נתונים.
- **Attention Heatmap** — מכשיר `SectionAttention` (שהיית IntersectionObserver → אירוע `section_attention`) עוטף את מקטעי המרכיבים / הטעם / הסיפור; מצרף ב-**`/admin/heatmap`** ("איזה חלק משך את העין").

**תוכנית Menu Intelligence: הושלמה.** כל השכבות חיות — Guest Experience, Behavior Intelligence, Menu Optimization. מיגרציות `0006`–`0009` הוחלו. 41 בדיקות יחידה. הפלטפורמה עונה כעת: מה האורחים רצו, מה משך אותם, היכן נשרו, ומה הבעלים צריך לעשות בהמשך.

---

## [2.4.0] — 2026-06-01

חיבור Supabase אמיתי הושלם — הסכמה פעילה, ומודל האבטחה אומת מול מסד הנתונים החי.

### בוצע

**מיגרציות הוחלו על הפרויקט החי** (`fihaaolredpqbdrdpuca`, eu-central)
- `0001_initial_schema`, `0002_storage_bucket`, `0003_cocktail_extras` רצו כולם ללא תקלות
- אומת: 6 טבלאות קיימות, ל-`cocktails` יש `price_ils`/`pairings`/`available_hours`, מסעדת ה-seed של Diner קיימת

**מודל אבטחת RLS אומת מול מסד הנתונים החי (בתפקיד `anon`)**
- משתמש anon קורא קוקטיילים שפורסמו → ✅ גלוי
- משתמש anon קורא טיוטות → ✅ מוסתר (0 שורות)
- משתמש anon כותב קוקטייל → ✅ נדחה עם קוד `42501` (הקוד הלא-ניתן-לחזרה `isTransient()` כבר מסווג נכון — אין סופת ניסיונות חוזרים)
- מאשר: התפריט הציבורי קריא ללא התחברות; **יצירה/קריאה של טיוטות מחייבת התחברות** (אבן הדרך הבאה)

### תוקן

**המסעדה מזוהה לפי slug, לא לפי UUID מקודד**
- ה-id של מסעדת ה-seed הוא ייחודי לכל מסד נתונים (`gen_random_uuid()`), ולכן ה-UUID המקודד `00000000-…0001` היה שגוי
- `SupabaseAdapter` מזהה כעת את ה-UUID מתוך ה-slug היציב `diner` ושומר אותו במטמון; הקשר הלוג ב-`store/index.ts` משתמש בסמן ה-slug

**חיבור זמן-ריצה פעיל ואומת**
- מפתחות anon ו-service_role הוצבו ב-`.env.local`; `isSupabaseConfigured()` → true → המפעל הוחלף ל-`SupabaseAdapter`
- אומת חי בדפדפן: `store initialized {provider: supabase}`, `draft.load.succeeded` תוך **337ms** (הלוך ושוב אמיתי ל-eu-central לעומת ~3ms ב-localStorage), מזהה הקורלציה נשמר, זיהוי slug→UUID עבד, דף הבית נטען עם 0 שגיאות
- משתמש anon יכול לקרוא `restaurants` (זיהוי slug) — אומת מול מסד הנתונים החי

### ממתין (דורש פעולת משתמש)

- **החלפת הסודות ששותפו בטקסט גלוי**: סיסמת מסד הנתונים, ה-JWT של `service_role`, וה-`sb_secret_…` (Settings → Database / API Keys). מפתח ה-anon/publishable בטוח לחשיפה ציבורית.
- יצירה/קריאה של טיוטות עדיין מחייבת התחברות (RLS) — אבן הדרך הבאה: התחברות סביב שיוך מסעדה (tenancy).

---

## [2.3.0] — 2026-06-01

מוכנות לאינטגרציה — מזהי קורלציה, טקסונומיית אירועים יציבה, סמנטיקת מצב "מלוכלך" (dirty), ומערכת הזרקת תקלות, כך שחיבור Supabase אמיתי יוכל להיבדק עם בדיקת כשלים מכוונת במקום בתקווה בלבד.

### נוסף

**טקסונומיית אירועים + מזהי קורלציה (`src/lib/observability/events.ts`)**
- שמות אירועים יציבים בנקודות (`draft.save.started/retry/succeeded/failed`, `draft.load.*`, `draft.delete.*`, `draft.publish.*`, `draft.find.*`) במקום לוגים בטקסט חופשי
- `OpContext` (requestId, op, restaurantId, slug, provider) משורשר דרך כל שרשרת פעולות באמצעות `newRequestId()` (crypto.randomUUID עם גיבוי)
- **אומת:** `draft.load.started` ו-`draft.load.succeeded` חולקים `requestId` אחד לאורך השרשרת האסינכרונית

**מערכת הזרקת תקלות (`src/lib/observability/faults.ts`)**
- לסביבות פיתוח/תצוגה מקדימה בלבד — לעולם לא מזריקה בייצור
- ממשק שליטה דרך קונסול `__faults.set('network'|'rls'|'slow')` / `.once()` / `.clear()`
- `network` → שגיאה זמנית (ניסיון חוזר מופעל); `rls` → קוד 42501 (ללא ניסיון חוזר); `slow` → השהיה (בדיקת מצב המתנה ב-UI)
- **אומת:** הזרקת `network` הפיקה `WARN[faults] injecting fault` → `ERROR[store] draft.load.failed`, ה-UI התדרדר בחן (ללא קפיאה/קריסה)

**סמנטיקת מצב מלוכלך (`useDrafts`)**
- `SyncStatus` לכל טיוטה: `saved | saving | dirty | sync_failed | retrying`
- עדכונים אופטימיים: טיוטה מופיעה מיד, מסומנת `saving`; בהצלחה `saved`; בכישלון העותק האופטימי נשמר ומסומן `sync_failed` (לעולם לא אובד בשקט)
- חושף `syncStatus`, `syncStatusFor(slug)`, `markDirty(slug)`

### שונה

- מתודות `CocktailStore` מקבלות `OpContext` אופציונלי; גבול ה-`instrument()` מייצר אותו, פולט אירועי טקסונומיה, ומריץ הזרקת תקלות
- `withRetry` נושא כעת `context` של קורלציה ופולט את אירוע `draft.save.retry`
- מתאמים (adapters) **זורקים כעת חריגה בכישלון כתיבה בלתי ניתן לשחזור** (במקום להחזיר טיוטה זמנית) כדי שמצב ה"מלוכלך" יוכל לזהות זאת; `useDrafts` שומר את העותק האופטימי ומסמן `sync_failed`
- `LocalStorageAdapter` זורק חריגה בכישלון כתיבה עקב מכסה (quota) או מצב פרטי

---

## [2.2.0] — 2026-06-01

יסוד תצפיתיות (observability) — לפני הוספת התחברות, נהפוך את שכבת השמירה (שכעת אסינכרונית) לניתנת לתצפית, כך שכשלים יצופו כאירועים במקום כסחף נתונים שקט.

### נוסף

**Logger מובנה (`src/lib/logger.ts`)**
- בעל רמות (debug/info/warn/error), מבוקר על ידי `NEXT_PUBLIC_LOG_LEVEL` (פיתוח: debug, ייצור: info)
- תלוי-הקשר דרך `logger.child({ scope })`; פלט הייצור הוא JSON בשורה אחת מוכן לאגרגטור
- ה-`console.*` היחיד המאושר בכל הקוד

**מכשור מתאמים (`store/index.ts`)**
- דקורטור עוטף כל `CocktailStore` כך שכל הקריאות מתעדות משך, גודל מטען, הצלחה/כישלון וספק — שני המתאמים מקבלים זאת ללא עלות
- אומת חי: `store initialized {provider}`, `getDrafts ok {ms}`, `saveDraft ok {slug, ms, bytes}`

**ניסיונות חוזרים עם השהיה (`src/lib/retry.ts`)**
- `withRetry` מנסה שוב כתיבות Supabase זמניות (network/5xx) עם השהיה אקספוננציאלית; שגיאות לוגיות (RLS, אילוצים) צפות מיד דרך `isTransient`
- מיושם על ה-upsert הקריטי של הקוקטייל ב-`SupabaseAdapter`

**גבול שגיאה (`src/components/ErrorBoundary.tsx`)**
- תופס קריסות בעץ-המשנה, מתעד אותן עם מחסנית הרכיב, ומציג חלופה חיננית
- הקנבס התלת-ממדי של `CocktailScene` עטוף ב**חלופה של תמונת גיבור סטטית** — קריסת WebGL מתדרדרת בחן במקום להציג דף ריק

**תיעוד ארכיטקטורה (`docs/architecture/`)**
- `storage-strategy.md`, `data-flow.md`, `rendering-architecture.md` — מתעדים את גבול המתאם, מחזור החיים של הטיוטה, וחוב הרינדור/ביצועים לפני שהמורכבות מצטברת

### שונה

- כל קריאות `console.error` המאולתרות במתאמי Supabase/localStorage הוחלפו בקריאות `logger` מובנות
- כשלי כתיבה עקב מכסה/מצב פרטי ב-`LocalStorageAdapter` מתועדים כעת במקום להיבלע בשקט

---

## [2.1.0] — 2026-06-01

יסוד השמירה (persistence) — שכבת מתאם אחסון שרצה היום על localStorage ועוברת ל-Supabase ברגע שהרשאות קיימות, ללא שום שינוי ב-UI.

### נוסף

**שכבת מתאם אחסון (`src/lib/store/`)**
- `interface.ts` — חוזה ה-`CocktailStore`: `getDrafts` / `saveDraft` / `deleteDraft` / `publishDraft` / `findDraft`
- `local.ts` — `LocalStorageAdapter` (ברירת מחדל; ה-backend הפעיל עד שתוגדר Supabase)
- `supabase.ts` — `SupabaseAdapter` שכותב על פני הסכמה המנורמלת (`cocktails` 1──< `cocktail_layers` / `cocktail_labels`), עם קריאות מקוננות והחלפת רשומות-בן בשיטת מחיקה-ואז-הכנסה
- `index.ts` — מפעל `getStore()`: בוחר ב-Supabase כאשר `NEXT_PUBLIC_SUPABASE_URL` מוגדר, אחרת localStorage
- `useDrafts` מאציל כעת למתאם — ה-API הציבורי שלו לא השתנה, כך שאף רכיב לא נדרש להשתנות מעבר להמתנה ל-`upsert`/`remove` שכעת אסינכרוניים

**מיגרציה `0003_cocktail_extras.sql`**
- מוסיפה את העמודות `price_ils`, `pairings` ו-`available_hours` כך שטבלת `cocktails` מייצגת במלואה את `CocktailConfig`
- `supabase/types.ts` נוצרו מחדש כדי להתאים

### תוקן

**שגיאות `never` בלקוח המוקלד של Supabase**
- בטיפוס ה-`Database` שנכתב ידנית חסר המפתח `Relationships: []` בכל טבלה, וכן מפתחות הסכמה `Views`/`Functions`/`Enums`/`CompositeTypes`
- בלעדיהם, האילוצים `GenericTable`/`GenericSchema` של supabase-js נכשלו וכל טיפוסי הארגומנטים של insert/update/upsert קרסו ל-`never`
- המפתחות הנדרשים נוספו — הכנסות מוקלדות נפתרות כעת כראוי

**מיגרציית טיוטות ישנות**
- טיוטות שנשמרו לפני הריפקטור השתמשו ב-`draftCreatedAt`/`draftUpdatedAt`; המתאם החדש מצפה ל-`createdAt`/`updatedAt`
- `LocalStorageAdapter.load()` משדרג כעת רשומות ישנות בעת הקריאה כך שטיוטות קיימות לא אובדות בשקט

### להפעלת Supabase

1. צור פרויקט Supabase
2. הרץ את `supabase/migrations/*.sql` לפי הסדר (עורך SQL או CLI)
3. הדבק את `NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_ANON_KEY` ב-`.env.local`
4. הפעל מחדש — המפעל מתחלף אוטומטית; ה-UI זהה

---

## [2.0.0] — 2026-06-01

שיפוץ מערכת העיצוב — אחידות ברחבי האתר, תיקון באג מפל CSS, ופריסת פירוט קוקטייל ברוחב מלא ב-3 אזורים.

### תוקן

**באג מפל CSS קריטי — כל כלי המרווחים היו מתים**
- איפוס `* { margin: 0; padding: 0 }` ללא שכבה ב-`globals.css` עקף בשקט כל כלי Tailwind בתוך `@layer utilities`
- התוצאה: כל `px-*`, `py-*`, `p-*`, `m-*`, `gap-*`, `mx-auto` חושבו ל-0 בכל האתר — שום דבר לא היה ממורכז, הכול נצמד לקצה
- התיקון: האיפוס הידני הוסר (Tailwind v4 Preflight כבר מאפס בתוך `@layer base` היכן שכלים עוקפים אותו כראוי)

**כרטיסי תפריט — זכוכית חופפת לתיאור**
- תמונת הגיבור ובלוק הטקסט מוקמו בצורה אבסולוטית ללא רצפה; כותרות בנות 2 שורות גרמו לחפיפה של 92–117 פיקסל
- הוחלף בפריסת flex אנכית: התמונה ממלאת את המקום הנותר למעלה, בלוק הטקסט יושב מתחת — חפיפה בלתי אפשרית מבחינה מבנית ללא קשר לאורך הכותרת
- אומת: כל 9 הכרטיסים מציגים כעת מרווח עקבי של ‎+11px בין התמונה לטקסט

**כרטיסי תפריט — יישור לא אחיד בין הכרטיסים**
- כותרות, מחירים וקישורי "Explore" נחתו בשורות שונות בכל כרטיס
- תוקן: בלוק הטקסט הוא כעת עמודת flex בגובה קבוע (`h-[244px]`) עם כותרת השומרת 2 שורות, תיאור 2 שורות, ושורת מחיר + explore המעוגנת לאותו קו בסיס באמצעות `mt-auto`
- אומת: 6 שורות שנמדדו נוחתות כולן באותן עמדות פיקסל מדויקות בכל 9 הכרטיסים

**ערכת הדפסה — "4 בעמוד" הוצג כ-2**
- ענף density=4 הפיק `grid-cols-2` (זהה ל-density=2)
- תוקן: density=4 מפיק כעת `grid-cols-2 lg:grid-cols-4` על המסך, ו-`print:grid-cols-2` (2×2 = 4 בגיליון A4)
- כמו כן תוקנה גלישת כרטיס קומפקטי (תוכן נחתך ב-26px) על ידי מעבר למידות לפי גובה התוכן עם `minHeight: 20rem`

### נוסף

**`AdminShell` — רכיב פריסת ניהול מאוחד**
- ניווט עליון דביק עקבי, h1 מוגבל, מפריד יהלום, מיכל ממורכז `max-w-6xl`, ו-CSS להדפסה
- יושם על כל דפי אזור הניהול: `/admin`, `/admin/new`, `/admin/[slug]/edit`, `/admin/import`, `/admin/print`, `/admin/qr`, `/admin/analytics`, `/changelog`

**פירוט קוקטייל — פריסת 3 אזורים ברוחב מלא (xl+)**
- עמודה שמאלית (260px): מכ"ם טעמים, הערת ברמן, התאמות
- עמודה מרכזית (flex-1): פירוק תלת-ממדי מפוצץ + תוויות מרכיבים
- עמודה ימנית (400px): תמונת גיבור בגודל מלא עם `mix-blend-screen` — הרקע השחור נמסך אל תוך הדף הכהה, והקוקטייל נראה צף
- תמונת גיבור: 344×459px (היה ברוחב 216px) — גדולה משמעותית
- היסט התווית הופחת ל-`5rem` מהמרכז כדי לפנות מקום לעמודת התמונה הרחבה יותר
- מתחת ל-xl: נופל חזרה לקנבס תלת-ממדי במסך מלא עם פאנל טעמים בפינה השמאלית-תחתונה (חוויית המובייל ללא שינוי)

**מחירים לכל הקוקטיילים**
- ל-6 קוקטיילים של Diner יש כעת מחירים זמניים (52–62 ₪) — ניתנים לעריכה דרך `/admin`
- כל 9 כרטיסי התפריט מציגים שורות מחיר עקביות על אותו קו בסיס

**טקסט מרכיבים — Citrus Lime Sour (משימה #100 הושלמה)**
- 6 תיאורים גנריים הוחלפו בטקסט עריכותי קולע התואם לסגנון שאר הקוקטיילים
- דוגמה: "The base spirit brings depth and a clean botanical character" → "London Dry — juniper-led, clean botanical backbone."
- התרגומים לעברית עודכנו בהתאם

### אומת

- ביקורת אתר מלאה: כל 14 הנתיבים מחזירים 200 (או 405 תקין עבור ממשקי API מסוג POST בלבד)
- מובייל (375px): רשת דף הבית, פירוט קוקטייל, AR — כולם נטענים כראוי
- TypeScript: `tsc --noEmit` נקי לכל אורך הדרך

---

## [לא שוחרר]

### מתוכנן (ספרינט 4 שלב B — חסום עד להגדרת המשתמש)
- חיבור `useDrafts` ל-Supabase במקום ל-localStorage
- דפי התחברות `/login` + `/signup` עם אימייל + Google
- קליטת מסעדה (הרשמה → יצירת מסעדה → הזמנת צוות)
- נתיבים רב-דיירים `/r/[restaurantSlug]/...`
- צינור אמיתי של תמונה→תלת-ממד (סגמנטציית SAM)
- אנליטיקה חיה (החלפת ה-mock בטלמטריה מטבלת `events`)

---

## [1.9.0] — 2026-05-28

ארכיטקטורת 7 שכבות עם דריסות פרומפט לכל קוקטייל. בעיית קליפת התפוז של ה-Aperol נפתרה סוף סוף.

### נוסף

**פונקציית עזר `makeCustomLayers(slug, overrides)`**
- כל קוקטייל יכול לדרוס את פרומפט היצירה לכל משבצת שכבה, או להסתיר משבצת לחלוטין
- נתיבי התמונות נגזרים אוטומטית מה-slug: `/cocktail/drafts/{slug}-{layer-id}.png`
- התנהגות ברירת המחדל נשמרת עבור קוקטיילים שלא דורסים דבר

**`splash_soda` — משבצת שכבה שביעית**
- מוכנסת בין `splash_clear` ל-`splash_pink` (ב-y=−0.75)
- תווית `Soda Water` חדשה נוספה ל-`APEROL_SPRITZ_LABELS`
- ממוספרת 06 (כך שכוס ה-Aperol Spritz הופכת ל-07)

**פרומפטים ייעודיים ל-Aperol לכל שכבה**
- 7 פרומפטים מותאמים תוכננו כדי לחמוק מההטיה של Pollinations לכיוון ירוק-ליים
- `lime_peel` → "Long curling ribbon of fresh orange citrus rind, sunset orange amber"
- `lime_wedges` → "Three round wheels of fresh blood orange citrus fruit"
- `splash_pink` → "Aperol liquor liquid splash crown, vivid amber-orange"
- `splash_clear` → "Prosecco golden champagne splash crown"
- `splash_soda` → "Pure transparent soda water splash crown"
- `glass` → "Tall stemless balloon wine glass filled with Aperol Spritz"
- התוצאה: שכבת קליפת התפוז **רוונדרה סוף סוף בכתום** ✓

**שכתוב ה-CLI** (`scripts/generate-cocktail-breakdown.ts`)
- כעת מבצע איטרציה על `cocktail.layers` במקום `SHARED_LAYERS` כך שפרומפטים לכל קוקטייל מכובדים
- מבטל את ה-`customizePrompt` הישן (שהוסיף הקשר פעמיים)
- קורא `layer.generationPrompt` ישירות

### שונה

**SHARED_LAYERS — נדחס ל-7 משבצות**
- הקנה מידה הופחת בכ-30% (זכוכית 0.95 → 0.7, התזות 0.85 → 0.55, קרח/חתיכות 0.45 → 0.35, קליפה 0.35 → 0.28)
- טווח ה-Y הודק ל-‎−1.4 עד 1.65 (3.05 יחידות, 73% מאזור התצוגה)
- כל 7 השכבות נכנסות בצורה נקייה עם שוליים של כ-0.3 יחידה מלמעלה ומלמטה
- אין חיתוך באף אזור תצוגה רגיל ביחס 16:9 או לאורך

### טכני
- 30 נתיבים, build תקין
- 4 קבצים חדשים שנוצרו ב-`public/cocktail/drafts/` עבור Aperol Spritz (קליפת תפוז, פלח תפוז, התזת סודה + יצירה מחדש של 3 הקיימים)
- כל שאר הקוקטיילים יורשים את SHARED_LAYERS ללא שינוי — Aperol הוא הקוקטייל הראשון המשתמש ב-`makeCustomLayers`. הדרך קדימה: להוסיף בהדרגה סטים של פרומפטים ייעודיים ל-Negroni, Pinky, Margarita וכו'.

---

## [1.8.0] — 2026-05-28

עיצוב תוויות עריכותי מחודש + פריסה נושמת — מקרבת את תצוגת הפירוט לכרטיס ה-Aperol Spritz הייחוס שהמשתמש שיתף.

### שונה

**תוויות**
- עיגולי המספרים `01/02…` הוסרו
- הכותרת היא כעת אותיות גדולות + מרווח אותיות צפוף ב-**orange-400** (תואם לדגש הייחוס), לא זהב-ענבר
- התיאור הוא Garamond נטוי (או Heebo עבור עברית), קטן וקליל יותר
- המחבר הוא נקודה כתומה זעירה + קו ענבר דק בגובה 36px המצביע לכיוון השכבה
- מצב פעיל: הכותרת מתבהרת, הנקודה גדלה + זוהרת, והמקור נחשף מתחת לתיאור

**מרווח שכבות** (`SHARED_LAYERS`)
- עמדות Y מפוזרות רחב יותר: glass ‎−1.35 · splash_clear ‎−0.6 · splash_pink 0.3 · ice 1.0 · lime_wedges 1.5 · lime_peel 1.9 (היה ‎−1.25 עד 1.4)
- הקנה מידה הופחת בכ-15% כדי להתאים לפיזור הרחב יותר ללא חיתוך (למשל glass 1.25 → 0.95)
- התוצאה: שטח לבן נדיב בין השכבות, קרוב יותר לייחוס העריכותי

### טכני
- 30 נתיבים, build תקין
- כל הקוקטיילים יורשים את המרווח החדש אוטומטית (Aperol עדיין מסיר 2 שכבות דרך filter)

---

## [1.7.2] — 2026-05-28

### תוקן
- **אי-התאמת ירוק-ליים ב-Aperol Spritz** — השכבות `lime_peel` ו-`lime_wedges` חזרו בירוק גם כשהקשר הקוקטייל ציין "Aperol, orange". Pollinations / Flux מוטים מאוד לכיוון דימויי ליים ירוק עבור כל פרומפט של "קליפה מסולסלת" או "פלח הדר", ומספר ניסיונות חוזרים (החלפת מודל ל-`flux-realism`, ניסוח מפורש של "blood orange / NOT lime / NOT green", החלפת מילות עוגן) — כולם החזירו את אותו פלט ירוק.
- פתרון עוקף: **להסיר את שתי השכבות הללו לחלוטין** עבור Aperol Spritz. 4 הנותרות (קרח, התזת Aperol, התזת Prosecco, זכוכית) נושאות את הסיפור בצורה נקייה. תוויות השכבות שהוסרו מדולגות אוטומטית מכיוון ש-`IngredientLabels` כבר מחזיר `null` כאשר לא ניתן לזהות את ה-`layerId` שלו.
- הדרך קדימה (נדחה): תבניות שכבה לכל קוקטייל כך שכל קוקטייל יוכל לבחור באילו משבצות להשתמש ומה לשים בהן — במקום שכל קוקטייל ינסה למלא את SHARED_LAYERS בעלות נושא ההדרים.

---

## [1.7.1] — 2026-05-28

### תוקן
- **תוויות לכל קוקטייל** — כל 9 הקוקטיילים השתמשו בעבר מחדש ב-`CITRUS_LIME_SOUR.labels`, כך שדפי ה-Aperol Spritz / Negroni / Margarita הציגו "Lime Peel · Persian lime, hand-zested" לצד דימויי תפוז/הדר/ויסקי. לכל קוקטייל יש כעת תוויות מוקלדות משלו התואמות למרכיביו האמיתיים:
  - **Aperol Spritz** — Orange Peel · Orange Slice · Ice Cubes · Aperol · Prosecco · Aperol Spritz
  - **Negroni** — Orange Peel · Orange Wheel · Ice Sphere · Sweet Vermouth · Campari · Gin
  - **Pinky** — Lime Peel · Fresh Raspberries · Crushed Ice · Raspberry Liqueur · Tonic Water · Pink Gin
  - **Margarita** — Salt Rim · Lime Wedge · Ice · Fresh Lime Juice · Triple Sec · Tequila Blanco
  - **Green Garden** + **Garden Spritz** — Fresh Basil · Cucumber Ribbon · Crushed Ice · Elderflower · Lime Juice · Sparkling Water
  - **Smoked Old Fashioned** — Orange Peel · Brandied Cherry · Clear Ice Sphere · Bitters · Demerara Syrup · Smoked Bourbon
  - **Whiskey Sour** — Maraschino Cherry · Lemon Peel · Ice · Egg White · Fresh Lemon Juice · Bourbon
- כל תווית כוללת מקור (למשל "Tanqueray London Dry", "Carpano Antica Formula", "Maker's Mark") כך שכותרת המשנה העריכותית נשארת נכונה לכל קוקטייל
- `GREEN_GARDEN_LABELS` + `SMOKED_OLD_FASHIONED_LABELS` הועלו מעל לשימוש הראשון בהם כדי לעמוד בבדיקת `used before its declaration` של TS

---

## [1.7.0] — 2026-05-28

ייצוא פוסטר סטטי — החלק החסר שהופך חוויית תלת-ממד למשהו שמסעדה יכולה לפרסם באינסטגרם או להדפיס ב-A3.

### נוסף

**`/api/poster/[slug]` — ייצוא סטטי עריכותי**
- מחזיר **PNG בגודל 1200×1500** המורכב באמצעות `next/og` ImageResponse
- טוען כל שכבת קוקטייל + תווית מהדיסק, ומטמיע כ-URLs של נתוני base64
- הפריסה משקפת את ייחוס "Components Breakdown": אותיות גדולות של קטגוריה · כותרת נטויה עם גרדיאנט · מפריד יהלום · כותרת משנה "Components Breakdown" · 6 שכבות מוערמות אנכית עם תוויות ממוספרות בצד ימין · כותרת תחתונה עם מותג + סלוגן
- עובד עבור כל קוקטייל ב-`MENU` עם שכבות ייחודיות (למשל `diner-aperol-spritz` לאחר שה-CLI לפירוק רץ)
- רינדור שרת טהור — אין צורך בהרכבה בצד הלקוח

**רכיב `PosterButton`**
- ממוקם ב-`CocktailScene` לצד Share / Ambient / AR
- לחיצה → מאחזר את ה-API → מוריד כ-`{slug}-poster.png`
- מציג "Building…" בזמן היצירה
- תווית דו-לשונית (Poster / פוסטר)

**שיקוף CLI של יצירת הפירוק** (`scripts/generate-cocktail-breakdown.ts`)
- `npm run generate:breakdown <slug>` משחזר את זרימת `/api/generate-breakdown` ללא צורך בשרת פיתוח פעיל
- שימש להפקת שכבות ה-Aperol Spritz הייחודיות וחיבורן ל-`DINER_APEROL_SPRITZ`

### שונה
- `DINER_APEROL_SPRITZ.layers` משתמש כעת בסט `public/cocktail/drafts/diner-aperol-spritz-*.png` שנוצר לאחרונה (פרופיל צבע כתום-ענבר התואם לזהות האמיתית של הקוקטייל)
- Build: 30 נתיבים (נוסף `/api/poster/[slug]`)

### טכני
- JSX של ImageResponse דורש סיומת `.tsx` — הנתיב נמצא ב-`src/app/api/poster/[slug]/route.tsx`
- כל נכסי התמונה מוטמעים כ-base64 — עובד בכל פריסה ללא URLs חיצוניים

---

## [1.6.0] — 2026-05-28

ערכת הדפסה — אוהלי שולחן עריכותיים שהמסעדה יכולה להדפיס ולהציב על השולחנות.

### נוסף

**דף `/admin/print`**
- פריסות כרטיס בודד עריכותיות (אחת לכל קוקטייל) שמודפסות כאוהלי שולחן
- כל כרטיס מכיל: עיטור יהלום · קטגוריה · שם גדול ב-Playfair נטוי · סלוגן · תמונת גיבור · ציטוט ברמן · QR + מחיר
- גיליון סגנון להדפסה (`@page A4 12mm margins`, `break-inside: avoid` לכל כרטיס, מסיר את כל המסגרת הכהה)
- **מתג צפיפות** — 1 / 2 / 4 כרטיסים בעמוד A4
- **מתג יעד QR** — תצוגת פירוק לעומת תצוגת AR
- **מתג מטבע** — אותו מעבר `₪ / $ / €` כמו במקומות אחרים, מוצג על הכרטיס המודפס
- תיבת סימון **כלול הערת ברמן** — מסתירה את הציטוט עבור פריסות צפופות יותר
- כפתור "Print sheet" מפעיל את דיאלוג ההדפסה המקורי עם גיליון הסגנון מוחל
- מקושר מכותרת `/admin` (`Print Kit →` ליד QR Codes)

### טכני
- משתמש מחדש בספריית `qrcode` (כבר מותקנת)
- משתמש מחדש בפונקציית העזר הקיימת `formatPrice` להמרת מטבע
- כל העבודה בצד הלקוח, ללא ממשקי API חדשים

---

## [1.5.0] — 2026-05-28

תכונות שיפור איכות חיים לסועדים ולמפעילים. תצוגת מחיר, צליל אווירה, היסטוריית צפייה, וסינון קיוסק לפי שעת היום.

### נוסף

**תצוגה רב-מטבעית**
- טיפוס `Currency` חדש (`ILS | USD | EUR`) + פונקציית עזר `formatPrice(priceILS, currency)`
- שערי חליפין סטטיים ב-`FX_FROM_ILS` (יוחלפו ב-API חי כשה-backend יהיה מוכן)
- שדה `priceILS?: number` ב-`CocktailConfig` (3 חתימות הבית מקבלות מחירים: ₪58 / ₪68 / ₪42)
- רכיב גלולה `CurrencyToggle` (שיקוף של LanguageToggle)
- hook בשם `useCurrency` עם שמירה ב-localStorage
- המתג נראה למעלה מימין בדף הבית; המחיר מוצג בכל כרטיס תפריט

**צליל אווירה** (`useAmbientSound`)
- זמזום גנרטיבי של Web Audio API — 3 מתנדי סינוס בכוונון מעט שונה ב-A2 (110 Hz) דרך מסנן מעביר-נמוכים
- כבוי כברירת מחדל, מופעל במחוות משתמש, העוצמה עולה בהדרגה לאורך 1.5 שניות לשיא של 0.08
- localStorage זוכר את ההעדפה בין ביקורים
- כפתור `AmbientToggle` (אייקון רמקול קטן עם X כשמושתק, גלים כשפעיל) — ממוקם ב-`CocktailScene` לצד Share + AR

**נצפו לאחרונה**
- hook בשם `useViewHistory` עם localStorage (8 הקוקטיילים האחרונים, ללא כפילויות, ממוין לפי זמן)
- `CocktailScene` רושם צפייה בעת הטעינה
- דף הבית מציג רצועת גלולות "Recently viewed" מעל המסננים כאשר יש היסטוריה כלשהי והמשתמש לא חיפש/סינן
- כל גלולה מקשרת ישירות חזרה לאותו קוקטייל (מטפל גם בקוקטיילים מקודדים וגם בטיוטות)

**לוח זמנים לקיוסק** (סינון לפי שעת היום)
- `availableHours?: [number, number]` חדש בקוקטיילים (עוטף מעבר חצות)
- מסנן `isAvailableNow(cocktail, currentHour)` ב-`/kiosk`
- Citrus Lime Sour: צהריים-חצות (12-24) · Smoked Old Fashioned: ערב בלבד (17-24) · Garden Spritz: כל היום (8-24)
- כותרת הקיוסק התחתונה מציגה את השעה הנוכחית + מספר הפריטים המוסתרים
- לחיצה על **S** בקיוסק מפעילה/מכבה את לוח הזמנים
- נופל אוטומטית חזרה ל-MENU המלא אם הסינון היה משאיר 0 פריטים

### שונה
- `MenuCard` מקבל כעת prop אופציונלי `currency` ומציג מחיר מתחת לסלוגן כשהוא מוגדר
- מקשי הקיצור של `/kiosk` הורחבו: `S` מפעיל/מכבה לוח זמנים, השאר ללא שינוי

### טכני
- 28 נתיבים (אין נתיבים חדשים; תוספות נתונים בלבד)
- כל התכונות בצד הלקוח, ללא תלויות חיצוניות

---

## [1.4.0] — 2026-05-27

לולאת QR + AR המלאה. סרוק קוד בשולחן → הקוקטייל מופיע צף בתצוגת המצלמה שלך.

### נוסף

**תצוגת AR** (`/ar/[slug]`)
- הזנת מצלמה (אחורית) כרקע במסך מלא דרך `getUserMedia`
- תמונת הגיבור של הקוקטייל צפה מעל הזנת המצלמה
- **גרירה באצבע אחת** למיקום מחדש · **צביטה בשתי אצבעות** לשינוי גודל (0.3-2.5×)
- כפתור **📸 Capture** — מרכיב פריים מצלמה + גיבור ל-PNG הניתן להורדה
- כפתור **Reset** משחזר את מיקום ברירת המחדל
- תג בפינה השמאלית-עליונה "AR · Live · {שם הקוקטייל}"
- מצבי הרשאה חיננים: `starting` / `ready` / `denied` (כפתור ניסיון חוזר) / `unsupported`
- מנקה ערוצי מצלמה בעת הפירוק (unmount)

**סורק QR** (`/scan`)
- סורק QR מבוסס מצלמה המופעל על ידי ספריית `qr-scanner` (עובד ב-iOS Safari)
- מזהה את כתובות האפליקציה שלנו: `/cocktails/...`, `/drafts/...`, `/ar/...`
- כתובות/מחרוזות אחרות מתעלמים מהן — ממשיך לסרוק
- מנתב אוטומטית כאשר נמצאת התאמה תקפה
- תצוגה מקדימה של "Last seen" מראה מה פוענח (עוזר באיתור באגים במדבקות/מסכים)
- אותה מכונת מצבים `starting/ready/denied/unsupported`

**ניווט**
- קישור `⌖ Scan QR` נוסף לכותרת התחתונה של דף הבית
- כפתור `View in AR` נוסף ל-`CocktailScene` (מובייל בלבד) לצד Share
- מחולל ה-QR `/admin/qr` כולל כעת מתג **Breakdown ↔ AR view** — כל QR מודפס יכול לקשר עמוק ישירות לחוויית ה-AR במקום לדף הפירוק

### טכני
- `qr-scanner` (^1.4.2) — ספרייה תואמת iOS שנבדקה
- Pointer Events API (עובד גם לעכבר וגם למגע) שימש למחוות ה-AR — ללא טיפול נפרד
- Build: 28 נתיבים (נוספו `/ar/[slug]` + `/scan`)

---

## [1.3.0] — 2026-05-27

ליטוש חוויית הסועד — התאמות, תוויות מובייל-תחילה, PWA, ג'ירוסקופ.

### נוסף

**התאמות**
- `pairings?: Localized[]` אופציונלי חדש ב-`CocktailConfig`
- 3 קוקטיילים מקודדים נזרעו עם הצעות התאמה (צדפות, ריב-איי, בורטה וכו')
- רכיב `Pairings` מציג "Pairs with · X · Y · Z" מתחת להערת הברמן בכתב סריף נטוי אלגנטי

**תוויות מובייל — גיליון תחתון** (`MobileLabelSheet`)
- מחליף את פאנל התוויות בצד ימין שהיה מוסתר במובייל
- כשהמשתמש מקיש על שכבה בסצנה התלת-ממדית, גיליון בטשטוש זכוכית מחליק מלמטה ומציג שם + מקור + תיאור
- כפתור × לסגירה; הגיליון נסגר אוטומטית כש-`activeLayerId` מתאפס
- תוויות הדסקטופ ללא שינוי (עדיין `hidden md:block`)

**תמיכת PWA**
- `src/app/manifest.ts` — manifest מבוסס-קובץ של Next 16 ב-`/manifest.webmanifest`
- `applicationName`, `appleWebApp.capable: true`, `statusBarStyle: 'black-translucent'`
- `viewport.viewportFit: 'cover'` לטיפול במגרעת ה-iPhone
- "Add to Home Screen" מפיק אפליקציה עצמאית בערכת צבעים שחורה

**הטיית ג'ירוסקופ** (`useGyroscope`)
- נרשם ל-`DeviceOrientationEvent`, מנרמל את `beta/gamma` ל-‎-1..1
- זרימת הרשאה ב-iOS 13+: מציג גלולת "Enable tilt" במובייל כשנדרשת הרשאה
- דפדפני אנדרואיד שאינם iOS מאפשרים אוטומטית
- ההטיה מוזנת ל-`CocktailLayers` דרך ref ו**מתווספת להיסט המצביע** ב-`useFrame` — כך שהשכבות מגיבות להטיית הטלפון בדיוק כפי שהן מגיבות לעכבר בדסקטופ
- מוכפלת ב-`parallaxFactor` הקיים של כל שכבה (שכבות אחוריות זזות פחות, שכבות קדמיות זזות יותר)

### טכני
- 27 נתיבים (נוסף `/manifest.webmanifest`)
- `tiltRef` מחובר מ-`useGyroscope` → `CocktailScene` → `CocktailLayers` → `useFrame` (ללא רינדורים מחדש; משנה את ה-ref ישירות ב-60 Hz)
- כל ארבע התכונות משוחררות ללא שירותים חיצוניים

---

## [1.2.0] — 2026-05-27

גרידה רב-פלטפורמית. כלי הייבוא מגיע כעת מעבר ל-getmood.io.

### נוסף
- **מנתח Wix Restaurants** — משתמש בתכונות יציבות `data-hook="menu-section"` / `menu-item` / `menu-item-name` / `menu-item-price` / `menu-item-description` / `menu-item-image`. מטפל גם בפריסות מקוננות (עוטף menu-section) וגם בפריסות שטוחות.
- **מנתח Tabit** — מאמץ מיטבי, קורא איי JSON מוטמעים בסגנון `window.__APP_DATA__`. רוב תפריטי Tabit נטענים דרך API ולכן התוצאות משתנות; נופל חזרה למנתח הגנרי.
- **גיבוי HTML גנרי** — מחלץ פריטי תגי h + מחירים סמוכים `\\d+ ₪ / shkalim / NIS / ש"ח`. עובד על תפריטים סטטיים פשוטים.
- **זיהוי פלטפורמה חכם** — בודק חתימות בסדר הזה: getmood.io → Tabit → Wix → גנרי (היוריסטיקה על ספירת כותרות + מחירים) → לא ידוע.
- **גיבוי בין-מנתחים** — אם מנתח ספציפי לפלטפורמה מחזיר אפס קטגוריות, המנתח הגנרי מנוסה לפני כישלון.
- **חילוץ תמונות** — מנתחים מחזירים כעת `image` לכל פריט כשזמין (תמונות פריטים של Wix, JSON של Tabit).

### שונה
- האיחוד `Platform` כעת: `'getmood.io' | 'wix' | 'tabit' | 'generic' | 'unknown'`
- טקסט העזרה של `/admin/import` עודכן כדי לפרט את כל הפלטפורמות הנתמכות

### טכני
- אומת ש-getmood.io נטול רגרסיות (Diner עדיין מנתח 75 פריטים)
- Build: 25 נתיבים (מספר ללא שינוי)

---

## [1.1.0] — 2026-05-27

תכונות תפעול מסעדה: תצוגת קיוסק, קודי QR לאוהלי שולחן, יצירת פירוק בכמות גדולה.

### נוסף

**מצב קיוסק** (`/kiosk`)
- מסך מלא, ללא מסגרת — נכנס ישירות ל-`CocktailScene` מסתובב
- קידום אוטומטי כל 12 שניות דרך כל קוקטייל ב-`MENU`
- קצה עליון: פס התקדמות בענבר (מתאפס לכל פריט)
- מונה בפינה הימנית-תחתונה `01 / 09`; רמזי עזרה בפינה השמאלית-תחתונה (`ESC` / מקשי חצים / רווח)
- מקלדת: `Esc` יוצא לתפריט, `←/→` מחליף, `Space` משהה
- כפתור "× close" בפינה הימנית-עליונה; קישור "Kiosk →" נוסף לכותרת הניהול

**מחולל קודי QR** (`/admin/qr`)
- QR אחד לכל קוקטייל (מקודד + טיוטות), המצביע לכתובת הציבורית שלו
- ערכת ענבר כהה (`#0a0a0a` על `#fde68a`), תיקון שגיאות גבוה (`H`)
- "Download PNG" לכל כרטיס + "Download all" גלובלי
- גיליון סגנון ידידותי להדפסה (`@media print`) — רקע בהיר, פריסת רשת, ללא מסגרת
- כפתור "Print sheet" מפעיל הדפסת דפדפן
- קישור "QR Codes →" נוסף לכותרת הניהול
- משתמש ב-`qrcode` (^1.5.4) ליצירה בצד הלקוח

**יצירת פירוק בכמות גדולה**
- רכיב `BulkBreakdownButton` חדש ב-`src/components/admin/`
- מוצג ב-`/admin` כאשר טיוטה אחת או יותר חסרות שכבות מותאמות
- עובר על הטיוטות ברצף; עבור כל אחת קורא ל-`/api/generate-breakdown` וקורא NDJSON עד הסוף, ואז מבצע `upsert` עם השכבות החדשות
- רשימת סטטוס חיה לכל טיוטה (`pending → in_progress → done / error`)
- מאשר לפני ההרצה (הערכה של ~75 שניות לטיוטה × N)
- מסתיר את עצמו כשלכל הטיוטות כבר יש שכבות מותאמות

### שונה
- כותרת הניהול קיבלה 2 קישורים חדשים (QR + Kiosk) — כעת 5 בסך הכול: Import / QR / Kiosk / Analytics / Build Log

### טכני
- 25 נתיבי build (3 ממשקי API דינמיים, 9 קוקטיילים סטטיים, 10 דפי ניהול/קיוסק/changelog וכו' סטטיים)
- `qrcode` + `@types/qrcode` הותקנו
- כל התכונות החדשות עובדות ללא Supabase (משתמשות ב-`MENU` + `useDrafts` של localStorage)

---

## [1.0.0] — 2026-05-27

חפיר הפלטפורמה. בעלי מסעדות יכולים כעת לקלוט את כל התפריט שלהם מכל כתובת נתמכת בלחיצה אחת, כאשר ה-AI מייצר תמונות גיבור לכל פריט.

### נוסף

**אשף ייבוא מסעדה בלחיצה אחת** (`/admin/import`)
- קלט כתובת + שם מסעדה → "Scan menu" → צ'יפים של קטגוריות בזמן אמת → תיבות סימון לכל פריט + עקיפת קטגוריה → "Import N items" עם התקדמות בהזרמה
- סטטוס לכל פריט (pending → in_progress → done / error) מתעדכן בזמן אמת במהלך היצירה
- ניחוש קטגוריה חכם (היוריסטיקה על מילות מפתח בעברית + אנגלית — whiskey/bourbon → smoky, citrus → citrus וכו')
- מנתב אוטומטית ל-`/admin` לאחר ההשלמה כך שהמשתמש רואה את הטיוטות הטריות שלו
- קישור "Import →" נוסף לכותרת הניהול (משמאל ל-Analytics)

**ספריית גרידה משותפת** (`src/lib/restaurant-scraper.ts`)
- `scrapeRestaurant(url)` → `ParsedMenu` (קטגוריות + פריטים + פלטפורמה + נוכחות תמונה)
- בשימוש על ידי סקריפט ה-CLI, ה-API לגרידה, ו-UI הייבוא — מימוש יחיד
- כרגע תומך ב-getmood.io; הוספת פלטפורמות נוספות היא פונקציה אחת

**ממשקי API**
- `POST /api/scrape-restaurant` — JSON: `{ url } → ParsedMenu | error`
- `POST /api/import-restaurant` — NDJSON בהזרמה: `start/done/error` לכל פריט + `complete` סופי עם הטיוטות
- כל פריט מיובא: יצירת גיבור ב-Pollinations → הסרת רקע בצד השרת → שמירה ל-`public/cocktail/drafts/{slug}-hero.png`
- הטיוטה שנוצרה כוללת שם, סלוגן (מהתיאור), קטגוריה, פרופיל טעם ברירת מחדל, והערת ברמן חתומה המייחסת את המסעדה המקורית

### טכני
- 22 נתיבי build (3 ממשקי API דינמיים, 9 קוקטיילים סטטיים, מספר דפי ניהול)
- סקריפט ה-CLI (`npm run import:restaurant <url>`) משתמש כעת אף הוא בספרייה המשותפת (ללא כפילות)

---

## [0.9.0] — 2026-05-27

קליטת המסעדה האמיתית הראשונה. הפלטפורמה מכילה כעת **9 קוקטיילים** — 3 חתימות בית + 6 אמיתיים שיובאו מ-**Diner** (https://www.dinerrest.co.il/).

### נוסף
- `scripts/import-restaurant.ts` — גורד תפריטי מסעדה גנרי. כרגע תומך ב-`getmood.io` (ה-SaaS שבשימוש Diner ומסעדות ישראליות רבות נוספות). מזהה פלטפורמה, מנתח קטגוריות + פריטים, מדפיס JSON מובנה. שמיש לכל כתובת: `npm run import:restaurant <url>`
- `scripts/fixtures/diner-menu.json` — תפריט Diner המלא המנותח (75 פריטים, 13 קטגוריות) + 6 קוקטיילים מועשרים עם שמות באנגלית/עברית, סלוגנים, פרופילי טעם, קטגוריות
- 6 קוקטיילים חדשים שפורסמו ב-`MENU`: `diner-aperol-spritz`, `diner-negroni`, `diner-pinky`, `diner-margarita`, `diner-green-garden`, `diner-whiskey-sour`
- לכל קוקטייל של Diner יש תמונת גיבור משלו שנוצרה על ידי AI (Pollinations + הסרת רקע) — 6 קבצי `.png` חדשים ב-`public/cocktail/`
- הערות ברמן חתומות "— Diner" כך שהן נקראות כקול של המסעדה

### ממצאים (הקשר חשוב)
- **ל-Diner אין שום צילום לכל מנה באתר** — זהו תפריט טקסט בלבד (שם, מחיר, תיאור). זו הנורמה עבור רוב המסעדות הבינוניות בישראל.
- השלכה מעשית: צינור ה-AI חייב לעבוד **ללא תמונות קלט**. יצירת הטקסט→תמונה הנוכחית כבר מטפלת בזה בצורה מושלמת.
- תכונת "תמונה אמיתית→פירוק תלת-ממד" (מבוססת GPT-4 Vision) הופכת לבעלת ערך רק עבור מסעדות שיש להן צילום מקצועי (נדיר).

### טכני
- כל 9 הקוקטיילים חולקים את `SHARED_LAYERS` כתבנית פירוק (יצירת פירוק לכל קוקטייל זמינה לפי דרישה דרך האשף אך אינה נוצרת מראש)
- ה-Build מעבד כעת מראש 9 נתיבי קוקטייל (3 מקודדים + 6 של Diner) — 15 נתיבים בסך הכול

---

## [0.8.0-prep] — 2026-05-27

הכנה לספרינט 4 שלב A. הסכמה + התשתית מוכנות; המשתמש צריך ליצור את פרויקט ה-Supabase ולשלוח מפתחות.

### נוסף

**סכמת מסד הנתונים** (`supabase/migrations/`)
- `0001_initial_schema.sql` — סכמה מלאה: `restaurants`, `restaurant_members`, `cocktails`, `cocktail_layers`, `cocktail_labels`, `events` + אינדקסים
- מדיניות Row Level Security (RLS) בכל טבלה — קוקטיילים שפורסמו הם ציבוריים, טיוטות מתוחמות לחברים, אירועים ניתנים להכנסה אנונימית
- אידמפוטנטי (`if not exists` / `drop policy if exists`) — בטוח להרצה חוזרת
- שורת seed: `{ slug: 'diner', name: 'Diner' }` כמסעדה הראשונה
- טריגר `updated_at` על `cocktails`

**דלי אחסון** (`supabase/migrations/0002_storage_bucket.sql`)
- דלי ציבורי `cocktail-assets`
- מוסכמה: `cocktail-assets/{restaurant_slug}/{cocktail_slug}/{layer_id}.png`
- RLS: קריאה ציבורית; חברים יכולים לכתוב רק לתיקיית המסעדה שלהם

**עוטפי לקוח Supabase** (`src/lib/supabase/`)
- `client.ts` — לקוח דפדפן דרך `createBrowserClient`, סינגלטון במטמון, זורק חריגה אם משתני הסביבה חסרים
- `server.ts` — לקוח שרת דרך `createServerClient` (מודע-עוגיות) + לקוח אדמין המשתמש ב-service role
- `types.ts` — סכמת `Database` מוקלדת התואמת ל-SQL
- פונקציית עזר `isSupabaseConfigured()` להתדרדרות חיננית

**תיעוד**
- `supabase/README.md` — הגדרה צעד-אחר-צעד, סקירת סכמה, RLS בשפה פשוטה, מוסכמת אחסון

**הגדרות**
- `.env.example` עודכן עם כתובת Supabase + מפתחות anon + service-role (מפתח Gemini נשמר כמורשת)

### טכני
- `@supabase/supabase-js@^2.106` + `@supabase/ssr@^0.10` הותקנו
- אתחול לקוח עצל — אין קריסה אם משתני סביבה חסרים (הדפים הקיימים ממשיכים לעבוד עם localStorage)
- ה-Build עדיין תקין עם 13 נתיבים

---

## [0.7.0] — 2026-05-27

ספרינט 4 שלב A. התקדמות בהזרמה חיה, אנליטיקה לבעלים, והעלאת תמונה.

### נוסף

**התקדמות פירוק בהזרמה**
- `/api/generate-breakdown` מזרים כעת אירועי **NDJSON**: `start` → 6× `layer-start`/`layer-done`/`layer-error` → `complete`
- האשף קורא את הזרם דרך `Response.body.getReader()` ומעדכן את המצב בזמן אמת
- רשת סטטוס חדשה של 6 באשף: כל שכבה פועמת בענבר תוך כדי תהליך, הופכת לאיזמרגד ✓ בסיום, ורוד × בשגיאה
- המשתמש יכול לראות "lime peel done · ice in progress" במקום ספינר אחד גדול

**לוח אנליטיקה לבעלים**
- `/admin/analytics` — 4 כרטיסי KPI (סך צפיות, הזמנות, המרה, פריט מוביל)
- שני גרפי sparkline ל-30 יום (צפיות בענבר, הזמנות בוורוד) באמצעות SVG מוטמע עם מילוי גרדיאנט
- טבלת פריטים מובילים עם צפיות / הזמנות / המרה לכל קוקטייל
- מפת חום של מעורבות ל-24 שעות (עמודות אנכיות, שיא עוצמה בערב + צהריים)
- כל המספרים מגיעים מנתוני mock דטרמיניסטיים כך שההדגמה יציבה; יוחלפו בטלמטריה אמיתית כשה-backend יחובר
- מקושר מכותרת `/admin` ("Analytics →")

**העלאת תמונה**
- מקטע הגיבור של האשף מציע כעת **"Upload photo"** לצד יצירת AI
- מקבל PNG / JPEG / WebP עד 4MB
- נקרא כ-data URL, מאוחסן ישירות בטיוטה (עובד עבור localStorage; יוחלף מאוחר יותר בהעלאת שרת + אחסון ענן)
- בעלי מסעדות עם תמונות מקצועיות משלהם יכולים להשתמש בהן ישירות

### שונה
- סך הנתיבים 14 (5 דינמיים, 9 סטטיים)
- צורת התגובה של `/api/generate-breakdown`: היה גוף JSON, כעת זרם NDJSON — הלקוח חייב לנתח שורה-אחר-שורה

### טכני
- NDJSON על פני SSE לניתוח פשוט יותר בצד הלקוח (אין צורך בספריית event-source)
- `Response` מחזיר `ReadableStream<Uint8Array>` עם `Content-Type: application/x-ndjson` + `X-Accel-Buffering: no` כדי להשבית באפרינג של פרוקסי
- מחולל נתונים דטרמיניסטי פסאודו-אקראי באנליטיקה להדגמה ויזואלית יציבה לאורך טעינות מחדש

---

## [0.6.0] — 2026-05-27

ספרינט 3 שלב C. הטיוטות ניתנות כעת לעריכה ולייצוא במלואן — הלוך ושוב עובד מקצה לקצה.

### נוסף

**עריכת טיוטות**
- `/admin/[slug]/edit` — נתיב דינמי הטוען כל טיוטה מ-localStorage וממלא מראש את הטופס
- משתמש מחדש באותו אשף, כולל יצירה מחדש של גיבור + יצירה מחדש של פירוק
- חלופה חיננית "draft not found" אם הכתובת מיושנת
- קישור "Edit" נוסף לכל כרטיס טיוטה ב-`/admin`

**טופס לשימוש חוזר**
- `src/components/admin/CocktailForm.tsx` — לוגיקת האשף חולצה כך ש-`/admin/new` ו-`/admin/[slug]/edit` חולקים אותה
- Props: `mode: 'new' | 'edit'`, `initial?`, `initialLayers?`, `initialHeroPrompt?`, `onSaved`
- ה-slug ניתן לעריכה במצב new, נעול במצב edit
- תווית כפתור השמירה מסתגלת ("Save cocktail" לעומת "Save changes")
- כפתורי גיבור / פירוק משנים תווית ל-"Regenerate" כשקיימים ערכים התחלתיים

**ייצוא בכמות גדולה**
- כפתור "Export all as JSON" ב-`/admin` — מעתיק את מערך הטיוטות המלא ללוח (מנוקה ממטא-נתונים פנימיים `draftCreatedAt`/`draftUpdatedAt`)
- "Copy JSON" לכל כרטיס — מעתיק JSON נקי של טיוטה אחת
- שניהם מציגים משוב "Copied!" למשך 1.5 שניות
- ה-JSON המיוצא נכנס ישירות לצורת `CocktailConfig` — שמיש לקידום טיוטה ל-`MENU` מקודד ב-`src/data/cocktail.ts`

### שונה
- `/admin/new/page.tsx` הוא כעת עוטף בן 30 שורות סביב `CocktailForm` (היה ~350 שורות)
- סך הנתיבים 13 (5 דינמיים, 8 סטטיים)

### טכני
- `CocktailForm` מקבל `initialLayers` אופציונלי כך שעריכות שומרות שכבות פירוק שנוצרו קודם
- פירוק עם קידומת קו תחתון (`{ draftCreatedAt: _c, ... }`) מונע אזהרות משתנה-לא-בשימוש תוך הסרת שדות פנימיים מ-JSON המיוצא

---

## [0.5.0] — 2026-05-27

ספרינט 3 שלב B. המוצר מייצר כעת פירוקי קוקטייל שלמים מתיאור יחיד, וכל דף קוקטייל מפיק תמונת שיתוף חברתי מותאמת.

### נוסף

**Open Graph (תמונות שיתוף חברתי)**
- `app/opengraph-image.tsx` — כרטיס ממותג לדף הבית (1200x630, "Our Cocktails" בשני גוונים עם מפריד יהלום, טקסט בגרדיאנט)
- `app/cocktails/[slug]/opengraph-image.tsx` — כרטיס OG דינמי לכל קוקטייל המציג כותרת + סלוגן + קטגוריה
- `generateMetadata` בכל דף קוקטייל מגדיר `openGraph.title/description` + מטא-נתוני כרטיס Twitter
- שיתוף קישור קוקטייל לאינסטגרם / וואטסאפ / Twitter / iMessage מפיק כעת תצוגה מקדימה יפהפייה

**צינור פירוק AI ל-6 שכבות (החפיר)**
- נתיב שרת `POST /api/generate-breakdown` — מקבל `{ slug, name, tagline, category }`, מייצר 6 שכבות ייחודיות דרך Pollinations + מסיר רקע מכל אחת דרך `@imgly/background-removal-node`, שומר ל-`public/cocktail/drafts/{slug}-{layerId}.png`, ומחזיר את מערך השכבות
- הפרומפט של כל שכבה מותאם להקשר הקוקטייל (שם, סלוגן, קטגוריה) כך שהצבעים והסגנון מסתגלים
- כשל-רך לכל שכבה — נופל חזרה לתמונת התבנית המשותפת אם שכבה בודדת נכשלת

**אינטגרציה לאשף**
- מקטע חדש ב-`/admin/new` — "3D Breakdown · 6-layer AI (optional)"
- כפתור "Generate full breakdown" — מושבת תוך כדי הרצה, המתנה של ~60-90 שניות
- טקסט התקדמות חי + אזהרה להשאיר את הלשונית פתוחה
- השכבות שנוצרו מוצגות ברשת של 6 לאישור ויזואלי
- "Regenerate breakdown" אם המשתמש רוצה וריאציה נוספת
- שכבות מותאמות נשמרות בטיוטה אם נוצרו; אחרת משתמשים ב-`SHARED_LAYERS`

### שונה
- `SHARED_LAYERS` מיוצא כעת מ-`src/data/cocktail.ts` כך שנתיב ה-API + האשף יכולים להשתמש מחדש בתבנית
- סך הנתיבים 12 (5 דינמיים, 7 סטטיים), כולל 2 נתיבי תמונת OG + נתיב API אחד

### טכני
- נתיב ה-API משתמש ב-`runtime: 'nodejs'` + `maxDuration: 300` כדי לאפשר יצירה ארוכת-טווח
- האשף שולח POST יחיד וממתין — ללא הזרמה עדיין (מתוכנן לשלב C)
- טיוטות שנוצרו נשמרות ב-`public/cocktail/drafts/` — בסדר לפיתוח מקומי, צריך אחסון ענן בייצור (נדחה)

---

## [0.4.0] — 2026-05-27

ספרינט 3 שלב A. המוצר הוא כעת CMS פעיל: צוות המסעדה יכול להרכיב קוקטיילים חדשים עם תמונות גיבור שנוצרו על ידי AI, לצפות בהם בתצוגה מקדימה, ולשתף קישורים.

### נוסף

**מעטפת CMS**
- `/admin` — דף ניהול מסעדה המפרט את כל הקוקטיילים המקודדים + טיוטות localStorage
- `/admin/new` — אשף מלא להרכבת קוקטייל חדש (שם EN/HE, סלוגן EN/HE, קטגוריה, דגלי תזונה, פרופיל טעם 0-5 לכל ציר, הערת ברמן EN/HE, יצירת גיבור AI)
- מחיקה-עם-אישור בכרטיסי טיוטה
- קישור "Preview" לכל טיוטה → `/drafts/[slug]`
- `/drafts/[slug]` — נתיב בצד הלקוח המרנדר כל טיוטת localStorage בחוויית הפירוק המלאה (מטפל ב-"not found" בחן)

**יצירת גיבור AI**
- `src/lib/heroPrompts.ts` — `buildHeroPrompt(input)` + `buildPollinationsUrl(prompt)` + `slugify(name)`
- כפתור "Generate with AI" באשף קורא ל-Pollinations ישירות; כתובת התמונה נשמרת בטיוטה (אין עיבוד בצד שרת עדיין)
- פרומפט מוצע אוטומטית מורכב משם + סלוגן; המשתמש יכול לערוך לפני היצירה

**שמירת טיוטות**
- `src/lib/useDrafts.ts` — hook מוקלד `useDrafts` עם שמירה ב-localStorage (מפתח `cocktail-demo:drafts`)
- `DraftCocktail = CocktailConfig & { draftCreatedAt, draftUpdatedAt }`
- API של `upsert` / `remove` / `findBySlug`
- `blankCocktailTemplate(slug)` — ברירות מחדל הגיוניות לטיוטות חדשות

**אינטגרציה לתפריט**
- טיוטות ממוזגות לתוך תפריט הבית לצד פריטים מקודדים
- נתיבי הכרטיסים מתפצלים: מקודד → `/cocktails/[slug]`, טיוטות → `/drafts/[slug]` (דרך prop חדש `isDraft` ב-`MenuCard`)
- ספירה חיה `02 / 04 · Volume I` משקפת את הסך הממוזג
- צ'יפים של סינון + חיפוש עובדים על פני מקודדים + טיוטות בצורה אחידה

**שיתוף**
- רכיב `ShareButton` — Web Share API עם גיבוי ללוח ("Link copied" toast בהצלחה)
- ממוקם ב-`CocktailScene` בפינה הימנית-עליונה לצד מתג השפה
- עובד על קוקטיילים מקודדים וטיוטות באופן זהה

**מסגרת ניווט**
- קישור "Admin →" נוסף לכותרת התחתונה של דף הבית לצד "Build Log →"

### טכני
- דפוס הידרציה של `useDrafts` (מחזיר דגל `hydrated` כך שהקוראים יכולים להימנע מהבהוב)
- טיוטות משתמשות בכתובות CDN של Pollinations ישירות — ללא אחסון מקומי של בתי התמונה (ידידותי למגבלת 5MB של localStorage)
- ה-Build כולל כעת את `/drafts/[slug]` כנתיב דינמי (`ƒ`) מכיוון ש-localStorage אינו זמין בזמן ה-build
- סך הנתיבים: 10 (1 דינמי, 9 סטטיים / מעובדים מראש)

---

## [0.3.0] — 2026-05-27

דחיפה משולבת של ספרינט 1 + 2. המוצר מרגיש כעת כמו תפריט רב-פריטי עם העשרת פריטים נאותה.

### נוסף

**שכבת נתונים**
- טיפוס `Category` (citrus / smoky / bitter / sweet / mocktail)
- `FlavorProfile` (sweet / bitter / citrus / smoky / herbal — סקאלה 0-5)
- `DietaryFlags` (vegan / glutenFree / alcoholFree)
- `IngredientLabel.origin` — שדה מותג / מקור מקומי אופציונלי
- `CocktailConfig.bartenderNote` + `bartenderName` + `flavor` + `category` + `heroPrompt`
- מילונים מקומיים `CATEGORY_LABEL`, `FLAVOR_LABEL`
- תבנית `SHARED_LAYERS` חולצה (3 קוקטיילים חולקים את אותו פירוק 6-שכבות לעת עתה)

**קוקטיילים חדשים**
- `Smoked Old Fashioned` — bourbon / oak / orange peel, קטגוריה מעושנת
- `Garden Spritz` — cucumber / basil / elderflower, מוקטייל נטול אלכוהול
- כל אחד עם תמונת גיבור משלו, פרופיל טעם, הערת ברמן, דגלי תזונה

**העשרת דף פריט** (`/cocktails/[slug]`)
- `FlavorRadar` — גרף מחומש SVG עם 5 צירים, ציור מונפש, מילוי גרדיאנט (פינה שמאלית-תחתונה)
- `BartenderNote` — ציטוט Playfair נטוי עם ייחוס (מרכז-תחתון)
- מקורות מרכיבים בשורה מתחת לכל שם תווית ("Tanqueray London Dry", "Pamplemousse Rose, Combier" וכו')

**דף הבית**
- רכיב `MenuFilters` — קלט חיפוש + צ'יפים של קטגוריות + מסנן מועדפים
- מקטעים מקובצים לפי קטגוריה כשמסנן "All" פעיל (לכל קטגוריה כותרת משלה)
- תצוגת רשימה מסוננת כשקטגוריה / חיפוש פעילים
- מצב ריק "No matching items"
- ספירה חיה `02 / 03 · Volume I` המבוססת על תוצאות מסוננות
- ספירת מועדפים בתווית הצ'יפ

**מועדפים**
- hook בשם `useFavorites` עם שמירה ב-localStorage
- כפתור לב בכל `MenuCard` (פינה ימנית-עליונה, זוהר ורוד כשפעיל)
- כפתור החלפה עוצר את ניווט ה-Link
- שורד טעינה מחדש ולאורך הפעלות

**יצירת נכסים**
- `scripts/generate-heroes.ts` — מייצר תמונות גיבור בלבד (נפרד מ-6 שכבות הפירוק)
- `npm run generate:heroes [slug...]` — מייצר את כל הקוקטיילים או ספציפיים
- תמונות גיבור של Smoked Old Fashioned + Garden Spritz נוצרו והוסר מהן הרקע

### שונה
- דף הבית ניתן כעת לגלילה (אנכית) כדי להכיל קוקטיילים מרובים
- טקסט פינה דקורטיבי בעמדה קבועה משתמש כעת ב-`fixed` במקום `absolute` כך שהוא נשאר במקומו בגלילה
- אינדקס הכרטיס משתמש ב-`MENU.indexOf(cocktail)` כך ש-"No. 02" נשאר נכון לאורך מסננים
- `CocktailScene` מוסיף מכ"ם + הערת ברמן לצד השכבות + התוויות הקיימות

### טכני
- `src/lib/useFavorites.ts` — hook מוקלד, בטוח מפני אחסון פגום, שומר מערך JSON של slugs

---

## [0.2.0] — 2026-05-27

תשתית יומן build ותיעוד בתוך האפליקציה.

### נוסף
- נתיב `/changelog` — יומן build בתוך האפליקציה הקורא את `CHANGELOG.md` בזמן ה-build ומרנדר בעיצוב יוקרתי
- `src/lib/parseChangelog.ts` — מנתח markdown לפורמט יומן השינויים
- קישור "Build Log →" בכותרת התחתונה של דף הבית

### שונה
- הוסר `overflow: hidden` גלובלי מ-`html, body` כך שדפים שאינם סצנה יכולים לגלול באופן טבעי
- הוסר `touch-action: none` מה-body (נשמר על הקנבס בלבד)
- הוסר `user-select: none` מה-body (נשמר דרך `select-none` במכלי הסצנה)

---

## [0.1.0] — 2026-05-27

ה-MVP הראשון הפעיל של חוויית פירוק הקוקטייל האינטראקטיבית.

### נוסף

**יסוד**
- מקור אמת יחיד `src/data/cocktail.ts` — קונפיגורציה מוקלדת לשכבות, תוויות, i18n
- `LayerConfig`, `IngredientLabel`, `CocktailConfig` בטוחי-טיפוס עם מחרוזות `Localized`
- מערך `MENU` + פונקציית חיפוש `findCocktailBySlug()`

**סצנה תלת-ממדית (תצוגת פירוק פריט)**
- פירוק שכבתי אנכי (זכוכית בתחתית → קליפת ליים למעלה)
- `CocktailScene` — קנבס Three.js, ממוקם דרך `next/dynamic` (ssr:false)
- `CocktailLayers` — שישה מישורים, כל אחד mesh תלת-ממדי עם טקסטורה
- `LuxuryLighting` — תאורה רכה אמביינטית + תאורת מפתח מכוונת מונפשת
- אנימציית ציפה לכל שכבה (sin/cos עם משרעת/מהירות עצמאית)
- תגובת פרלקס לעכבר עם lerp כבד לתחושה קולנועית
- זיהוי ריחוף על השכבות דרך אירועי מצביע של R3F

**ריחוף דו-כיווני (שכבה ↔ תווית)**
- ריחוף מעל שכבה תלת-ממדית → התווית התואמת זוהרת
- ריחוף מעל תווית → השכבה התלת-ממדית התואמת מתבהרת (פולטת אור) ומתרחבת מעט
- מצב מיקוד — כאשר שכבה כלשהי פעילה, השכבות האחרות דוהות ל-20% שקיפות לאפקט זרקור קולנועי

**תוויות**
- `IngredientLabels` — פאנל שכבת-על של שמות + תיאורים, ממוקם ליד כל שכבה
- פריסת מראה: תוויות אנגלית משמאל לשכבות, תוויות עברית מימין (מודעת-RTL)
- זוהר drop-shadow על שם התווית הפעילה
- עיגולי מספרים + קווי מחבר הוסרו לפי משוב עיצובי (מראה נקי יותר)

**צינור יצירת נכסים**
- `scripts/generate-assets.ts` — מייצר 6 קבצי PNG שקופים של שכבות מפרומפטים טקסטואליים
- משתמש ב-**Pollinations.ai** (חינמי, ללא מפתח) עם מודל Flux
- הסרת רקע דרך `@imgly/background-removal-node` (מקומי, חינמי, אלפא שקוף)
- תמיכת ארגומנט CLI — `npm run generate:assets -- splash_clear` מייצר מחדש שכבה אחת
- פרומפטים לכל שכבה נוסחו כדי להימנע ממיכלים/משטחים ("isolated subject on pure black void")

**בינאום (EN / HE)**
- רכיב `LanguageToggle` — מתג בסגנון גלולה EN / עב בפינה הימנית-עליונה
- כל תוכן הטקסט מתורגם: כותרות, כותרות משנה, שמות תוויות, תיאורים, סלוגנים
- החלפת גופן לכל שפה: Playfair Display (EN) ↔ Frank Ruhl Libre (HE); EB Garamond (EN) ↔ Heebo (HE)
- `dir="rtl"` + `lang="he"` במכלי הטקסט בעברית
- מיקום התווית משתקף בהתאם לשפה

**דף נחיתה של התפריט**
- דף בית חדש `/` — נחיתת תפריט קולנועית
- ריפקטור נתיבים: פירוק הפריט הועבר ל-`/cocktails/[slug]`
- `generateStaticParams` לעיבוד מראש סטטי של פריטי התפריט
- `CocktailSceneClient` — עוטף דק המאפשר דפי Server Component עם סצנה בייבוא דינמי
- רכיב `MenuCard` — כרטיס תלת-ממדי נוטה עם תמונת גיבור, זוהר הילה, השתקפות מתחת
- אפקט ברק עוקב-עכבר בריחוף מעל הכרטיס
- עיטור מספר עמוד "No. 01" + מפריד יהלום בכל כרטיס

**אפקטי רקע קולנועיים (`BackgroundFX`)**
- 18 חלקיקי אווירה צפים (גדולים, מטושטשים, נסחפים לאט — "אבק יוקרה")
- 3 גרדיאנטים רדיאליים נושמים (ורוד / ענבר / כחול קריר) לצבע אווירה
- זרקור עדין עוקב-עכבר (רדיוס 1000px, אלפא 0.035 — כמעט תת-מודע)
- שכבת-על של גרגרי סרט (6% שקיפות)
- גרדיאנטי וינייטה עליון/תחתון

**מסגרת הדף**
- מרקיזה עריכותית איטית למעלה (מחזור 120 שניות, מרווח 0.7em, שקיפות 0.25)
- עיטורי פינה אנכיים: "Est. 2026 · House Signature" + "01 / 01 · Volume I"
- כותרת תצוגה בשני גוונים — "Our" עם גרדיאנט לבן→ענבר→ברונזה + "Cocktails" ב-Playfair נטוי
- מפרידי עיטור יהלום לכל אורך הדף

**טיפוגרפיה**
- Google Fonts דרך `next/font/google`: Playfair Display, EB Garamond, Inter (לטינית)
- Frank Ruhl Libre, Heebo (עברית)
- כל המשתנים חשופים כ-CSS custom properties

### שונה
- קני המידה של השכבות הודקו פעמיים על בסיס משוב עיצובי כדי להיכנס במלואם לאזור התצוגה
- עמדות ה-Y הוזזו 0.25 יחידות מטה ליותר מרחב נשימה מעל הקליפה
- ספירת החלקיקים הופחתה לחצי (36 → 18), הוגדלו, טושטשו, האטו
- אנימציית המרקיזה הואטה פי 3; השקיפות הופחתה
- עוצמת זרקור העכבר הופחתה בכ-פי 2 לריסון קולנועי

### הוסר
- מתג הקש-לפיצוץ (במקור רדיאלי — הוחלף בפריסה אנכית סטטית)
- עיגולי מספרים 01-06 על התוויות (מראה נקי יותר בלעדיהם)
- קווי מחבר בין המספר לתמונה
- תלות `@fal-ai/client` (הוחלפה ב-Pollinations חינמי + rembg מקומי)

### טכני
- Next.js 16.2.6 (Turbopack)
- React 19.2.4
- React Three Fiber 9 + Drei + Three.js 0.184
- Framer Motion 12
- Tailwind CSS 4
- TypeScript 5 (strict)
- `@google/genai` (מותקן אך לא בשימוש — נשמר לשימוש עתידי בתשלום ב-Nano Banana)
- `@imgly/background-removal-node` ליצירת PNG שקוף
- `dotenv` + `tsx` לסקריפטי יצירת נכסים
